# Build-Phasen K+ Onepager

Jede Phase hat eine eigene Datei in `docs/phases/` mit Ziel, Aufgaben, Definition of Done und Prüfschritten. Eine Phase ist erst fertig, wenn alle DoD-Punkte abgehakt sind und `npm run build` grün ist. Danach: Status hier eintragen, committen, nächste Phase.

Aufruf in Claude Code, z. B.: `Lies CLAUDE.md und docs/phases/PHASE-0-setup.md und führe Phase 0 aus.`

| Phase | Inhalt | Ergebnis | Status |
|---|---|---|---|
| 0 | [Setup](phases/PHASE-0-setup.md) – Astro, Tokens, Fonts, Assets, Base-Layout, Content-Loader | Leere Seite mit Fonts, Meta, JSON-LD; Build grün | fertig |
| 1 | [Rahmen](phases/PHASE-1-rahmen.md) – Nav, Hero, Footer, Impressum/Datenschutz-Seiten | Seite mit Kopf und Fuß, Anker funktionieren | fertig |
| 2 | [Vision · Mission · Marquee](phases/PHASE-2-vision-mission-marquee.md) | Drei statische Sektionen | fertig |
| 3 | [Warum K+](phases/PHASE-3-warum-kplus.md) – Karten-Slider (statisch scrollbar) | Sektion Desktop + Mobile | fertig |
| 4 | [Portfolio](phases/PHASE-4-portfolio.md) – Logo-Wall + Spotlight mit Umschaltung | Sektion inkl. Klick-Logik | fertig |
| 5 | [Schwerpunkte · Founder · CTA](phases/PHASE-5-schwerpunkte-founder-cta.md) | Restliche Sektionen statisch | fertig |
| 6 | [Responsive](phases/PHASE-6-responsive.md) – Mobile-Frame 1:1, Zwischen-Breakpoints | Alle Sektionen bei 390 / 768 / 1024 / 1440 / 1920 sauber | fertig |
| 7 | [Animationen](phases/PHASE-7-animationen.md) – GSAP, Lenis, Hex-Cursor, reduced-motion | Alle 15 Animationen aus der Liste | fertig – Teil A (2–6, 8, 11–14) · Teil B (1, 7, 9, 10, 15); Test auf echtem Mobilgerät offen |
| 8 | [SEO · Performance · Deploy](phases/PHASE-8-seo-deploy.md) | Lighthouse ≥ 95, Sitemap, OG, GitHub Action → IONOS | offen |

## Reihenfolge und Abhängigkeiten

- 0 → 1 → 2 → 3 → 4 → 5 sind sequenziell (jede Sektion baut auf den Tokens/Komponenten davor auf).
- 6 (Responsive) erst nach 5, damit alle Sektionen einmal komplett stehen. Innerhalb der Phasen 1–5 wird trotzdem schon der Mobile-Aufbau grob angelegt (Spalten stapeln), Feinschliff in 6.
- 7 (Animationen) strikt nach 6 – nie Animationen in eine Sektion einbauen, bevor sie statisch abgenommen ist.
- 8 kann teilweise parallel vorbereitet werden (Deploy-Action ohne Secrets), Livegang erst nach Freigabe.

## Abnahme pro Phase (Standard-Prüfung, gilt für alle)

1. `npm run build` ohne Fehler und Warnungen.
2. `npm run preview`, Seite bei 1920 und 390 px Breite ansehen (Playwright-Screenshot in `docs/screens/phase-N-*.png` ablegen, falls Playwright verfügbar ist, sonst manuell).
3. Kein Text hart im Code – Stichprobe `grep -rn "Build beyond" src/` darf nur `de.json`-Zugriffe treffen.
4. HTML-Validität: eine `h1`, `h2` je Sektion, `alt` an allen Bildern.
5. Status in dieser Tabelle aktualisieren, Commit mit Präfix der Phase, z. B. `feat(phase-3): warum k+ section`.

## Status-Log

_(Claude Code trägt hier je Phase Datum, Commit-Hash und Besonderheiten ein.)_

### Phase 0 – Setup · 2026-09-09 · Commit `bec2aed`

- Stack: Astro 5.18.2 (CLAUDE.md: „neueste 5.x"), @astrojs/sitemap 3.7.4, sharp 0.35.4, gsap 3.15, lenis 1.3.26; Dev: @types/node, @astrojs/check, typescript. `npm run build` und `npm run check` grün.
- **Hinweis Astro-Version:** `npm audit` meldet für alle Versionen ≤ 7.2.7 eine kritische Advisory-Sammlung; die 5.x-Linie erhält keine Fixes mehr (aktuell 7.3.2). Für den statischen Build nicht akut, Upgrade-Entscheidung auf 7.x vor Phase 8 (Deploy) treffen.
- Icons Warum-K+: `building`, `project`, `network` per Figma MCP als SVG exportiert und bereinigt (viewBox 0 0 50 50, `currentColor`). `partnership` (Node 139:85) und `capital` (Node 75:528) sind im Figma nur Rasterbilder in Maskengruppen → Platzhalter-Kreise mit TODO; die PNGs (512×512, transparent) liegen in `_material/figma-export/`.
- K+-Marke: Figma Node 139:34 enthält nur ein PNG → `public/logos/kplus.png` als Interim (674×370, transparent), `favicon.svg` = teal Quadrat mit TODO. SVG-Marke beim Kunden anfordern.
- Logos: `Place.png` und `vynci.png` haben entgegen ASSETS.md **keinen Alphakanal** (weißer Hintergrund) → `scripts/prepare-images.mjs` stellt Weiß frei. `i-pro.jpeg` bleibt farbig (`mask: false` in `de.json`). Alle Logo-Pfade in `de.json` auf die realen Endungen gesetzt, `nav.logo` ergänzt.
- Fonts: 5 WOFF2 in `public/fonts/`, Preload Regular + Medium. Prüfung ohne Playwright (nicht installiert): Preview-Server per Astro-API gestartet, alle Font-URLs mit HTTP 200 / `font/woff2` bestätigt. Kein Screenshot in `docs/screens/`.
- Scaffold-Hinweis: `npm create astro .` legte trotz `.` einen Unterordner an; Dateien wurden in den Root verschoben.

### Phase 0 – Upgrade auf Astro 7 · 2026-09-09 · Commit `b54cf28`

- Astro 5.18.2 → **7.3.2** nach den offiziellen Guides „Upgrade to v6" und „Upgrade to v7" (`npx @astrojs/upgrade` bricht nicht-interaktiv an der Rückfrage ab → `npm install astro@7.3.2`). @astrojs/sitemap 3.7.4 und @astrojs/check 0.9.10 waren bereits die zu 7.x passenden Versionen; sharp 0.35.4 entspricht Astros optionaler Abhängigkeit.
- Relevante Breaking Changes geprüft: Node ≥ 22.12 (vorhanden: 24.14), Vite 8 (keine eigenen Plugins), Rust-Compiler (JSON-LD-`<script>` explizit geschlossen; keine ungeschlossenen Tags), `compressHTML: 'jsx'` als neuer Default (Inline-Elemente in Templates auf einer Zeile halten, Hinweis in CLAUDE.md), Sätteri-Markdown und `@astrojs/db` nicht betroffen, keine `experimental`-Flags in der Config.
- Ergebnis: `npm run build` grün (1 Seite, Sitemap), `npm run check` 0 Fehler / 0 Warnungen, `npm audit` **0 Schwachstellen** (vorher 3: 1 critical, 1 high, 1 low). CLAUDE.md nennt als Stack jetzt die aktuelle Major-Version (7.x).

### Phase 1 – Rahmen · 2026-09-09 · Commit `b689449`

- Neu: `Nav.astro`, `Hero.astro`, `Footer.astro`, `Button.astro` (Props `href`, `label`, `variant`; restliche Attribute werden auf das `<a>` durchgereicht), Layout `Legal.astro` mit den Seiten `/impressum` und `/datenschutz` (Platzhaltertext, `noindex` über neue Base-Prop), `src/lib/images.ts` (`resolveImage()` mappt `de.json`-Pfade `/images/*` auf `src/assets/images/` für astro:assets; `publicImageSize()` liest Logo-Maße für `width`/`height`).
- `de.json` ergänzt: `nav.ariaLabel`, `footer.linksLabel`, `legal.*` (Titel, Meta, Platzhalter, Zurück-Link) sowie der Zeilenumbruch `\n` in `hero.text` nach dem ersten Satz (Spec Abschnitt 1, Figma).
- Tokens ergänzt: `--c-night-a30/-a40/-a85`, `--c-teal-a40`, `--nav-top`, `--nav-height`, `--nav-height-scrolled`, `--scroll-offset` (= `scroll-padding-top` 96 px in global.css).
- Hero: `<Picture>` mit AVIF/WebP (768–2560 px), `loading="eager"`, `fetchpriority="high"`, `width`/`height` aus der Quelle; Verläufe als `::before`/`::after` (je 50 % Höhe = 469/936). Mobile: Outline-Zeile ohne 10-px-Tracking (Mobile-Frame 242:22), Buttons volle Breite / 46 px, Block unten ausgerichtet (Buttons enden bei 725 wie im Frame). Feinschliff der Mobile-Abstände in Phase 6.
- K+-Logo-PNG per `sharp().trim()` auf 481×165 beschnitten (der Figma-Export hatte transparenten Rand) → in der Nav 175×60, Spec 174×60.
- Hinweis: `resolveImage()` nutzt ein eager `import.meta.glob`; dadurch landen derzeit auch ungenutzte Original-JPGs in `dist/_astro/` (~1,2 MB). Ab Phase 5 sind alle Bilder in Verwendung.
- Abnahme: `npm run build` (3 Seiten) und `npm run check` grün. Neues Skript `npm run check:phase -- 1 index impressum` (`scripts/phase-check.mjs`): startet `astro preview` als IPv4-Daemon, Lighthouse-Accessibility je Seite für Desktop 1920 und Mobile 390 (Screen-Emulation), Full-Page-Screenshots nach `docs/screens/` (gitignored). Ergebnis: index **100/100**, impressum 96/100, datenschutz 96/100. Einziger Abzug (`color-contrast`): Nav-Button Magenta `#FF00FF` auf Night = 4,17:1, AA verlangt 4,5:1 für 16-px-Text – Designwert aus dem Manual; auf dem Hero-Foto nicht messbar, auf flachem Hintergrund (Rechtsseiten) schon. → Hinweis an Design (Option: leicht helleres Magenta oder größere Schrift), kein Code-Workaround.
- Gelernt (Windows): Chrome `--headless=new` hängt mit `--virtual-time-budget`; Chrome erzwingt ~480 px Mindest-Fensterbreite (390er-Screenshots nur per Emulation); `astro preview` ist in Astro 7 ein Daemon (`astro preview stop`); die `preview()`-JS-API ignoriert `server.host` und lauscht nur auf `::1`.
- Nachtrag `fix(phase-1)` (2026-09-09): Token `--c-magenta-text: #FF4DFF` für Magenta-Text auf Night (Eyebrows, Text der Primär-Buttons inkl. Nav-CTA; Rahmen, Glow und Aktiv-Zustände bleiben `--c-magenta`) → Kontrast 4,84:1, dokumentiert als Abweichung 8 in DESIGN-SPEC.md. `.gitattributes` mit `* text=auto eol=lf` und Binär-Regeln, Zeilenenden normalisiert (nur `kappes-group.svg` war CRLF; `prepare-images.mjs` schreibt SVGs jetzt mit LF). Hero-Eyebrow bei 14 px KMR Apparat Bold per 2×-Screenshot geprüft: rendert sauber – die Doppelung im Figma-Screenshot ist ein Artefakt versteckter Layer.

### Phase 2 – Vision · Mission · Marquee · 2026-09-09 · Commit `73a402c`

- Neu: `Vision.astro` (Grid 778/702, gap 120, `<Picture>` 342–1556 px lazy, `object-position: 60% 50%`, Mobile 1 Spalte mit Bild 342×260 zuerst), `Mission.astro` (Grid 702/778, gap 203, drei Prinzipien mit `border-left` lime, gap 50 / Mobile 24, `data-reveal-stagger`), `Marquee.astro` (dekorativ `aria-hidden`, zwei Zeilen à 55 px, Einträge je Zeile doppelt gerendert für die Endlos-Animation in Phase 7, Zeile 1 `flex-end` / Zeile 2 `flex-start`, Outline-Einträge Bold mit `--outline-color: var(--c-teal)`, Fade-Maske 522 px / Mobile 60 px, `data-marquee-direction`).
- Abstände laut Spec: Hero → Vision 80, Vision → Mission 80, Mission → Marquee 80, danach `--section-gap`. Zwischenbreakpoint < 1440: Spalten proportional (`minmax`), gap 64 – Feinschliff in Phase 6.
- `index.astro`: Platzhalter `vision` durch die drei Komponenten ersetzt; die restlichen Anker bleiben Platzhalter.
- Abnahme: Build (3 Seiten) und Check grün, `npm run check:phase -- 2 index`: Lighthouse a11y **100/100** Desktop und Mobile, Screenshots in `docs/screens/phase-2-*`. Vision-/Mission-Positionen im Screenshot (1015 / 1760 / 2150) decken sich mit den Figma-Y-Werten (1016 / 1746 / 2139).
- Nachtrag `fix(phase-2)` (Commit `ea5ecfc`): Marquee-Outline-Einträge rendern jetzt hohl (`color: transparent`, 2 px Teal-Stroke, Bold) – das scoped `.marquee__item` hatte die globale `.outline-text`-Regel überstimmt. Abgleich mit Figma 139:59.

### Phase 3 – Warum K+ · 2026-09-09 · Commit `22243ed`

- Neu: `WarumKplus.astro` – Kopf (Eyebrow max. 163 px, H2, Slider-Label muted 16/26,4, `align-items: flex-end`), Karten-Track `<ul role="list" tabindex="0" aria-label>` mit `overflow-x: auto`, `scroll-snap-type: x mandatory`, `scroll-padding-inline-start: var(--gutter)`, Karten 636 px, gap 32, Trailing-Padding `calc(100% - gutter - 636px)`, damit auch die letzte Karte am Startrand einrastet. Fünf Feature-Karten (Icon 50 × 50 als inline SVG aus `public/icons/warum/` zur Buildzeit gelesen, `currentColor` = `--c-magenta`; gap 100; H3 `--fs-h3-card` teal; Text 18/28) + Statement-Karte (32/1,25 teal zentriert). Glow 384 px, `blur(32px)`, Gradient `--c-lime-a10` bei 50 % Opazität (Hover ×2 = 100 %), Hover-Rahmen `--c-teal-a30` nur bei `hover: hover`.
- Fortschrittslinie: Basis `--c-teal-a30`, Füllung **magenta** – Figma-Linie 139:188 ist #FF00FF, der Spec-Text „teal" war ein Extraktionsfehler (in DESIGN-SPEC.md korrigiert). Zähler 01/06 muted. Inline-Modul-Script (Astro-Bundle, kein GSAP) setzt `--progress` (kontinuierlich aus `scrollLeft`) und den Zähler (Karte, deren Start dem Snap-Rand am nächsten liegt); ohne JS 1/6 und 01/06.
- Mobile (< 768): Track als Grid, gap 12, Karten 342, padding 28, Icon 40, Abstände Icon → 20 → Titel → 20 → Text (Figma 242:87), Glow 230 px bei −40/−120, Slider-Label und Fortschritt ausgeblendet. Tablet (768–1023): Karten 480, padding 40, weiter scrollbar.
- `de.json`: `warumKplus.trackLabel` als `aria-label` des Tracks ergänzt. Tokens: `--c-teal-a30`, `--c-lime-a10`, `--fs-small`, `--lh-small`.
- Abnahme: Build/Check grün, `npm run check:phase -- 3 index` a11y **100/100**. Funktionstest mit neuem `scripts/browser-eval.mjs` (DevTools-Protokoll über Nodes WebSocket, kein Playwright): Desktop `scrollLeft` 0 → 01/06 · Karte 3 → 03/06 / Progress 0,5 · Ende → 06/06 / 1,0, kein horizontaler Body-Overflow; Mobile Grid 342 px, Icon 40, Fortschritt `display: none`.
- Nachtrag `fix(phase-3)`: Karten-Track startet oberhalb von 1920 px bündig mit dem zentrierten Container (`--track-start: max(gutter, (100% − container) / 2)` für Padding und `scroll-padding`); geprüft bei 2560 px (Kopf und erste Karte bei 473 px).
- Nachtrag `feat(phase-3)`: `partnership.svg` (zwei verschränkte Ringe) und `capital.svg` (Münze mit Euro-Zeichen) als selbst gezeichnete Stroke-Icons (`stroke-width` 1,4 = Linienbreite der Figma-Icons, 1,37 Einheiten), Strichstärke per 4×-Render und 2×-Screenshot neben `building.svg` verglichen. Damit keine Platzhalter mehr; Details in ASSETS.md.

### Phase 4 – Portfolio · 2026-09-09 · Commit `ab998f7`

- Neu: `Portfolio.astro` (Kopf max. 974 px, Logo-Wall `role="tablist"` 3×2 mit `<button role="tab">`-Tiles 240 px hoch, Spotlight-Panels `role="tabpanel"` – alle sechs gerendert, nur das aktive ohne `hidden`) und `src/scripts/portfolio.ts` (WAI-ARIA-Tabs: Klick, Pfeil links/rechts, Home, End mit Wrap, Roving-Tabindex, `focus({ preventScroll })`, `CustomEvent('portfolio:change')` für Phase 7). Ohne JS: erstes Spotlight sichtbar, Tiles inert.
- Logos: Teal-Maske per `mask: var(--logo) center/contain` mit `aspect-ratio` aus den Bildmaßen (`publicImageSize()` zur Buildzeit, keine Maße in `de.json`). `prepare-images.mjs` trimmt jetzt transparente Ränder (PLACE 554×106, VynciTech 692×167), sonst zählt die Maskenbox den Rand mit und die Marken wirken zu klein. **Kappes + Kemper** ist als Maske unbrauchbar (die K-Buchstaben sitzen im opaken Kasten → „▇ APPES EMPER") → wie im Figma unmaskiert (`mask: false`), SVG nachfordern; i-pro Kom ebenfalls unmaskiert.
- Aktiv-Zustand ohne Layout-Shift: 1 px Rahmen magenta + `inset 0 0 0 .5px` Schatten (= 1,5 px) + Glow `--c-magenta-a35`; Hover `translateY(-4px)`, Rahmen `--c-magenta-a50`. Spotlight `grid 680px 1px 1fr`, `min-height` 480, Textspalte Padding 31/56 und gap 24 (statt 56/28), damit auch das längste Panel exakt 480 px hoch bleibt und das Umschalten die Seite nicht verschiebt. „Webseite"-Button nur bei URL (derzeit Kappes Group), `target="_blank" rel="noopener"`.
- Mobile (< 768): Wall 2×3 mit Tiles 165×128 (Figma 165×128), Logos max. 118×36, Spotlight gestapelt mit Bild 200 px oben und horizontaler Accent-Line; Tablet 2 Spalten, Bild 280.
- Tokens: `--c-magenta-a90/-a50/-a35`, `--c-tile-bg-active`, `--c-white-a70`, `--c-night-a95/-a20`. `de.json`: `portfolio.wallLabel`.
- Abnahme: Build/Check grün. `browser-eval`: Klick → `aria-selected`/`hidden`/`tabindex` korrekt, ArrowRight/End/Wrap, vier `portfolio:change`-Events, Fokus folgt; alle Spotlights 480 px, kein Scroll-Versatz beim Umschalten; Mobile 2 Spalten, 165×128, Bild oben, kein Overflow. Lighthouse a11y Desktop **96**, Mobile **100** – einziger Abzug: „Webseite"-Button (`--c-magenta-text` #FF4DFF) auf dem teal-getönten Spotlight-Hintergrund = 4,08:1 (auf Night 4,84:1). Option: Token auf #FF66FF (4,5:1 auf der Tönung, 5,4:1 auf Night) – Entscheidung offen.
- Nachtrag `fix(phase-4)` (2026-09-09, Commit `da0ed44`): Spotlight unter 1024 px war auf die 200 px des Bildes abgeschnitten – durch `order: -1` am Bild rutschte die Textspalte in die feste 1-px-Grid-Zeile der Trennlinie, `overflow: hidden` klemmte Text, Chips und Button ab. Jetzt `height: auto` ohne feste Zeilen (Bild `order: -2`, Linie `order: -1` mit `height: 1px`). Geprüft per `browser-eval` (neue Option `--screenshot=<png>`) bei 390 px: Kappes Group 819 px hoch (Bild 200, Linie 1, Text 616), „Webseite"-Button vollständig im Panel; Place Strategy ohne Button 615 px; 768 px: 679 px mit Button; Desktop unverändert 480 px; kein horizontaler Overflow. Crops in `docs/screens/fix-4-spotlight-390-*.png`. Token `--c-magenta-text` auf `#FF66FF` (Night 5,36:1, Spotlight-Tönung 4,51:1, damit AA auch für den „Webseite"-Button), Abweichung 8 in DESIGN-SPEC.md aktualisiert.

### Phase 5 – Schwerpunkte · Founder · CTA · 2026-09-09 · Commit `4ef747d`

- Neu: `Schwerpunkte.astro` (Klassen `.focus*`), `Founder.astro`, `Cta.astro`; `index.astro` ohne Platzhalter in der Reihenfolge Hero → Vision → Mission → Marquee → WarumKplus → Portfolio → Schwerpunkte → Founder → Cta. Sektionsabstand weiterhin per `margin-top: var(--section-gap)` je Komponente wie in den Phasen 1–4 – die im Phasen-Doc genannte `.section`-Utility mit `padding-block` würde den Abstand zwischen zwei Sektionen verdoppeln und alle Bestandskomponenten anfassen.
- Schwerpunkte: `<Picture>` 1373 px breit zentriert (768/1373/2000, AVIF/WebP, lazy), vier Verläufe zu Night als ein Layer mit vier `linear-gradient`s (oben 445, unten 395, links/rechts 960 × 538 an den unteren Ecken, Stops 3,84 % / 13,45 % wie Figma 139:382–385), Overlay `--c-night-a60`. Kopf 850 zentriert bei 63, Spalten `repeat(2, minmax(0, 460px))` mit `space-between`, 52 px unter dem Kopf (Figma: Kopf 63–177, Items 229); `<h3>` 24 Bold lime, Text 18/28 teal, Items gap 50, je Liste `data-reveal-stagger`, Hintergrund `data-parallax`; `min-height` 832 (Figma-Frame). Unter 1024: eine Spalte linksbündig gap 24, Bild `cover`, Overlay `--c-night-a70`, ohne Seitenverläufe (zwei 960-px-Verläufe würden bei 390 das Foto decken), `padding-block` 72 (der Mobile-Frame hat 72 innen); unter 768 Kopf linksbündig (242:211).
- Kontrast Schwerpunkte (DoD): Hintergrund unter den Textspalten im 1920-Screenshot mit dekodiertem Foto gesampelt (Text ausgeblendet): hellster Pixel Luminanz 0,072 → teal 4,86:1, lime 7,33:1, Kopf 4,84:1 – über 4,5:1.
- Founder: Grid 713/827 gap 60 `align-items: center`, Porträt `aspect-ratio 713/515`, `object-position 50% 15%`; Text Eyebrow → 24 → Name (`.h2`) → 10 → Rolle lime → 24 → vier Absätze gap 16. Unter 1440 proportional (`minmax(0, 713fr)` / `827fr`), unter 1024 gestapelt mit Bild 342/247, Radius 8 (242:235) und `object-position 50% 20%`, unter 768 gap 28.
- CTA: Glow 775 px `radial-gradient(circle, --c-lime-a10, transparent 70%)`, blur 32, top −130, `data-glow`; Display `--fs-display-cta` Heavy, zweite Zeile `.outline-text` mit `--ls-outline` (10 px, Figma 139:465) und Überlappung `calc(size / -6)` (= −20 bei 120, −6 bei 36); Subline max. 1006 teal per `nl2br`; Buttons gap 16, Sekundär ohne Teal-Füllung (`:global(.cta__secondary)`, Hover weiß). Mobile: Glow 426 / −40 / blur 17,6 (242:248), Outline-Tracking `--ls-outline-mobile` 3 px, Buttons 342 × 46 gap 12.
- Spec-Korrektur: Mobile-CTA-Display **36 px** statt 46,8 – die 46,8 im Spec war die Zeilenhöhe (36 × 1,3; Figma 242:252); mit 46,8 px brach „Let's build beyond." bei 390 px um. Token `--fs-display-cta: clamp(36px, 14.588px + 5.4902vw, 120px)`, Typo-Tabelle und Abschnitt 9 angepasst.
- Tokens: `--ls-outline-hero` → `--ls-outline` (Hero und CTA), neu `--ls-outline-mobile`, `--c-night-a60`, `--c-night-a70`.
- Abnahme: Build (3 Seiten) und Check grün, keine Platzhalter, `grep "Build beyond" src/` trifft nur den Marquee-Kommentar. `browser-eval` 1920: Schwerpunkte 4519–5351 (832), Founder 5471–6110 (639), CTA 6230–6676 – gegenüber Figma (4509 / 5461 / 6219) konstant +10 px aus Phase 4; CTA 446 statt 516 hoch, weil das Display laut Spec Zeilenhöhe 1,0 hat (Figma: leading normal); Founder-Bild 713 × 515, CTA-Zeilen 120 px mit 20 px Überlappung, Buttons gap 16 transparent, eine `h1`, je Sektion eine `h2`, alle Bilder mit `alt`, kein horizontaler Overflow bei 1920/390. 390: Kopf links, sechs Items einspaltig, Porträt 342 × 247, Display 36 px einzeilig, Buttons 342 × 46. Lighthouse a11y Desktop **96**, Mobile **96** (`npm run check:phase -- 5 index`) – einziger Abzug weiterhin der „Webseite"-Button im Portfolio: axe rundet den getönten Hintergrund auf `#313F3F` und kommt mit `#FF66FF` auf 4,49:1 (rechnerisch 4,51); `#FF67FF` wäre der kleinste Wert, der auch bei axe 4,5:1 erreicht – Entscheidung offen. Screenshots: `docs/screens/phase-5-index-{desktop,mobile}.png` (Lighthouse – lazy geladene Fotos fehlen dort), `phase-5-schwerpunkte-1920.png`, `phase-5-founder-1920.png`, `phase-5-cta-390.png`.
- Gefunden, nicht Teil dieser Phase: bei 1024 px (mit Scrollbar 1009) läuft die Seite horizontal über – Verursacher ist allein die Portfolio-Wall (drei `1fr`-Spalten, Logos bis 314 px + Padding → Min-Content 1118 > Container 881). Für Phase 6 vormerken (z. B. `minmax(0, 1fr)` und `max-width: 100%` am Logo).
- Tooling: `browser-eval` mit `--timeout` (Watchdog) und `--screenshot-mode=viewport`. Full-Page-Captures hingen in Chrome, sobald große dekodierte AVIF-Fotos sichtbar waren (nach `scrollIntoView` oder mit Viewport in Seitenhöhe); Viewport-Captures nach `scrollTo` + `img.decode()` laufen zuverlässig.
- Nachtrag `fix(phase-5)` (2026-09-09): `--c-magenta-text` auf `#FF67FF` – kleinster Wert, der auch mit axe-Rundung des getönten Spotlight-Hintergrunds (`#313F3F`) 4,5:1 erreicht (4,51:1; Night 5,39:1, Tönung rechnerisch 4,53:1). Abweichung 8 in DESIGN-SPEC.md aktualisiert.

### Phase 6 – Responsive · 2026-09-09 · Commit `4365e10`

- Grids: alle festen Pixel-Spalten (Vision 778/702, Mission 702/778, Founder 713/827, Spotlight 680/919) laufen unterhalb 1920 proportional (`minmax(0, Xfr)`), weil der Container schon ab 1919 px schmaler als 1600 ist – bei 1440 lief die Seite vorher 403 px über (Vision). 1024–1439: Gutter 64, Warum-Karten 560, Spotlight-Textspalte 45 %, Schwerpunkte-Spalten 40 % / 40 %. 768–1023: Vision, Mission, Founder und Spotlight gestapelt, Wall 2 Spalten, Schwerpunkte einspaltig, Nav-Links ausgeblendet (CTA-Button bleibt; Burger-Menü weiter offen).
- Portfolio-Wall bei 1024 (Auftrag 1): `repeat(3, minmax(0, 1fr))`, Logos `max-width: min(314px, 100%)` + `object-fit: contain` – vorher zog die Min-Content-Breite der Logos (bis 314 px + Padding) die `1fr`-Spalten auf 1118 px.
- 10-px-Versatz (Auftrag 2): Ursache war die Fortschrittszeile von Warum K+ – der Zähler 18/28 machte die Zeile 28 px hoch, im Figma ist der Rahmen 139:185 11 px hoch (Text läuft über). Jetzt `height: 11px`; Warum K+ 600 statt 617 (Figma 607). Folge-Sektionen bei 1920: Portfolio 3105 (Figma 3109), Schwerpunkte 4502 (4509), Founder 5454 (5461), CTA 6213 (6219), Footer 6851 (6855) – der Rest von −4 bis −7 px kommt aus der Spec-Zeilenhöhe 1,2 für H2/H3 (Figma: normal ≈ 1,31, Kopf 109 statt 113).
- CTA-Höhe (Auftrag 3): bestätigt – die 70 px fehlten, weil das Display laut Spec Zeilenhöhe 1,0 hat, Figma aber „normal" (≈ 1,3, 155 px je Zeile bei 120 px). Ausgleich als `padding-block: calc(var(--fs-display-cta) * 0.3)` am Titel (2 Zeilen × 0,3 em, je zur Hälfte oben und unten) → CTA 518 (Figma 516), mobil Titel 88 (Figma 87,6); Subline und Buttons liegen damit auf ±1 px an Figma. Zeilenhöhe bleibt 1,0.
- Mobile 390 (Figma 242:21): Kopf-Abstände Eyebrow → H2 → Body 16 statt 24 über das neue Token `--gap-head` (24 / mobil 16), Sektionsabstand 72 auch für Vision, Mission und Marquee (vorher 80), Hero-Gaps 20 und Zeilenüberlappung −6, Wall/Spotlight-Abstände 32, Spotlight-Text gap 20 und Logo 45, Founder-Textgaps 16, CTA `padding-block` 96 und Subline → Buttons 24, Footer (Logo → 16 → Claim → 32 → Linie → 32 → Adresse → 32 → Links → 12 → Copyright, 40 unten, keine zweite Linie). Bildhöhen Hero 780, Vision 260, Spotlight 200, Founder 247, Tiles 165 × 128, Buttons 46 – alle 1:1.
- Sektionshöhen bei 390 gegenüber dem Figma-Inhalt: Hero ±0, CTA +14, Footer +34, Vision +116, Mission +102, Portfolio +128, Schwerpunkte +179, Warum K+ +193, Founder +285 – ausschließlich Typografie: Body im Mobile-Frame Inter 15/23 und H3 20, laut Spec 18/28 und 22, dazu läuft KMR Apparat breiter (mehr Zeilen). Die ±8 px der DoD sind damit nur strukturell erreichbar; Option 16/26 („Mobile ggf. 16/26" laut Spec) bleibt eine Kundenentscheidung.
- Unter 360 px: Body 16/26 (Phasen-Doc), Display 68 statt 78 („Beyond." war bei 320 px um 11 px abgeschnitten) und CTA-Display 30. Hero mobil `min-height: min(780px, 100svh)`; Landscape 844 × 390: Hero 535 hoch, Textblock vollständig sichtbar, kein Overflow.
- Touch und Tastatur: Nav-Links 50 px hoch (Padding 12,5), Logo-Link mobil 44 (Padding 4), Footer-Links 44 (Padding 8 mit Margin −8, kein Layoutunterschied). Tab-Reihenfolge Logo → Hero-Buttons → Karten-Track → aktives Tile → Spotlight → Webseite → CTA-Buttons → Footer-Links, alle mit Lime-Ring – `:focus-visible` per echten Tab-Tastendrücken über CDP bei 390 und 1920 geprüft (Script-`focus()` löst den Ring nicht aus).
- Fluid Type: die Formeln aus dem Phasen-Doc (`clamp(78px, 6.25vw, 120px)`, `clamp(34px, 2.2vw, 42px)`) nicht übernommen – sie ergäben bei 1024–1439 Display 78 und H2 34 statt der Spec-Werte 96/36 und bei 1440 H2 34 statt 42; die linearen Clamps aus Phase 0 treffen die Spec-Tabelle (1024: 95,4 / 37,3).
- `sizes`: Vision `(min-width: 1024px) 778px, 100vw`, Founder 713px, Spotlight 920px; Hero 100vw.
- Overflow-Check `scrollWidth === innerWidth` (Chrome `--hide-scrollbars`) bei 320, 390, 768, 1024, 1440, 1920 und 2560: überall wahr; mit klassischer Scrollbar `scrollWidth === clientWidth` bei 1024, 1440 und 1920.
- Tooling: `scripts/lib/cdp.mjs` (gemeinsamer DevTools-Helfer, freier Port aus `DevToolsActivePort`, Watchdog), `scripts/page-shot.mjs` (Full-Page-Screenshot aus Viewport-Slices mit geladenen und dekodierten Bildern – Chromes Full-Page-Capture hängt bei großen AVIFs), `browser-eval` darauf umgestellt (neu `--scrollbars=hidden`), `phase-check` misst jetzt Accessibility, Best Practices und CLS.
- Abnahme: Build und Check grün; Screenshots `docs/screens/phase-6-{320,390,768,1024,1440,1920,2560}.png` (page-shot, mit Fotos), Sichtprüfung 768/1024/1440/2560 ohne Auffälligkeiten; Lighthouse (`npm run check:phase -- 6 index`, jetzt mit Best Practices und CLS): Desktop a11y **100**, Best Practices **100**, CLS 0,000; Mobile a11y **100**, Best Practices **100**, CLS 0,001 – der „Webseite"-Button ist mit `#FF67FF` nicht mehr gelistet.
- Nachtrag `fix(phase-6)` (2026-09-09): Body unter 768 px als Token **16/26** (`--fs-body`/`--lh-body` im 767er-Block von tokens.css, nicht mehr nur < 360) – gilt für alle Body-Ableitungen (Karten-, Prinzip-, Schwerpunkt-, Spotlight-Text, Subline, Rolle, Footer); Typo-Tabelle im Spec (Body, Footer-Text, Warum-Mobile) angepasst. Sektionshöhen bei 390 erneut gegen den Figma-Frame 242:21 gemessen (Element-Höhe gegen Frame-Inhalt ohne den 72er-Abstand; Schwerpunkte/CTA mit Innenabstand, Footer ab Marke): Hero 780/780 ±0 · Vision 741/697 **+44** (vorher +116) · Mission 691/607 **+84** (+102) · Marquee 90/90 ±0 · Warum K+ 1724/1591 **+133** (+193) · Portfolio 1551/1505 **+46** (+128) · Schwerpunkte 1020/923 **+97** (+179) · Founder 1319/1210 **+109** (+285) · CTA 594/588 **+6** (+14) · Footer 404/382 **+22** (+34). Rest kommt aus H2/H3-Zeilenhöhe 1,2 (Figma 1,0 im Mobile-Frame) und KMR Apparat, die breiter läuft als Inter 15/23 (mehr Zeilen) – nicht strukturell. Mobile-Navigation entschieden: bleibt ohne Burger-Menü (PROJEKT-STRUKTUR.md Punkt 10, Spec-Abweichung 7).

### Phase 7 – Animationen, Teil A (Scroll-Grundlagen) · 2026-09-09 · Commit `2e054b4`

- Umfang Teil A (Auftrag): Punkte 2, 3, 4, 5, 6, 8, 11, 12, 13, 14. Teil B nach Sichtung: 1 (Hero-Typo), 7 (Slider), 10 (Spotlight-Wechsel), 15 (Hex-Cursor). **Punkt 9 (Logo-Wall-Stagger) ist keinem Teil zugeordnet** – Entscheidung offen.
- Neu: `src/scripts/animations.ts` – ein Einstieg, in `Base.astro` als Modul gebündelt (**49 kB gzip** inkl. GSAP + ScrollTrigger + Lenis, DoD ≤ 90). Startzustände nur per `gsap.from`/`fromTo`, nichts im CSS. `gsap.matchMedia()` mit zwei Kontexten: `(prefers-reduced-motion: no-preference)` → Lenis + Marquee; `{ reduce, desktop ≥ 1024, hover }` → Reveals, Parallax, CTA-Glow, Karten-Glow (nur `hover: hover`), bei `reduce` früher Return. Ein Wechsel der Bedingungen zur Laufzeit setzt alles zurück (Elemente sichtbar, ohne Transform). Der Nav-Zustand (3) läuft immer – Zustand, keine Bewegung.
- 2 Hero-Parallax: `<picture>` (`data-parallax`) `scale 1.1 → 1`, `y 0 → 120` über die Hero-Höhe, scrub; mobil nur `y 0 → 60`.
- 3 Nav: `.is-scrolled` ab 40 px per ScrollTrigger (`start: 40, end: 'max'`), Transition 300 ms bleibt CSS; aktiver Link als `aria-current="true"` (CSS von `.is-active` umgestellt) – aktiv, solange die Sektion unter der Viewport-Mitte liegt (Start `top center`, Ende `top center` der Folgesektion, damit zwischen zwei Sektionen keine Lücke ohne aktiven Link entsteht).
- 4 Section-Reveal: `data-reveal` an Vision (Bild + Text), Mission-Text, Warum-Kopf, Portfolio-Kopf + Spotlights, Schwerpunkte-Kopf, Founder (Bild + Text), CTA-Inhalt – zehn Ziele: `y 24 → 0, opacity 0 → 1`, 0,8 s, `power4.out` (= `--ease-out` cubic-bezier(.22, 1, .36, 1)), `start: 'top 85%'`, `once`. Mobil y 16.
- 5 Mission-Prinzipien: `[data-reveal-stagger] > *` stagger 0,12.
- 6 Marquee: Track mit **vier Kopien statt zwei** – bei zwei wäre der Track bei 1920/2560 px kürzer als Zeile + Periode und der Loop hätte eine Lücke – plus `padding-right` = gap 50, damit eine Kopie exakt ¼ der Trackbreite ist → `xPercent` ±25 → 0, `repeat: -1`, 40 s / 32 s, `ease: none`. Zeile 1 bleibt rechtsbündig (läuft nach links, von +25), Zeile 2 linksbündig (nach rechts, von −25) – statischer Zustand ohne JS/reduced motion wie im Spec. Velocity-Boost bis ×1,5 (`ScrollTrigger.getVelocity()`, über den Ticker gedämpft, nach 150 ms Ruhe zurück auf 1). Pause bei `document.hidden`.
- 8 Karten-Glow: Opazität ×2 bleibt CSS-Hover; per `gsap.quickTo` x/y ±30 px zum Cursor (`pointermove`), nur `(hover: hover)`.
- 11 Schwerpunkte: Hintergrund (`data-parallax`) `y −60 → 60` scrub (mobil ±30); Listen `data-reveal-stagger="left|right"` → `x −24 / +24` (mobil y 16).
- 12 CTA-Glow: Puls `scale 1 → 1.12, opacity .8 → 1`, 6 s yoyo; Drift `y −40 → 40` über die Sektion. Die Zentrierung liegt jetzt in der CSS-Eigenschaft `translate` statt `transform`, damit GSAP `transform` allein besitzt.
- 13 Buttons: `.btn:active { transform: scale(.98) }`, Transition 200 ms.
- 14 Lenis (`lerp .1`, `wheelMultiplier 1`, `syncTouch false`), Kopplung `lenis.on('scroll', ScrollTrigger.update)` + `gsap.ticker` (`lagSmoothing(0)`). Anker über eigenen Click-Handler (`pushState`, Fokus auf die Sektion mit `tabindex=-1`, `lenis.scrollTo(target)`) – **ohne `offset: -96`**: Lenis 1.3 zieht `scroll-padding-top` (96 = `--scroll-offset`) selbst ab, geprüft (Sektion landet bei 96 px). Lenis-CSS in global.css (`html.lenis`, `.lenis-smooth { scroll-behavior: auto !important }`, `main > section:focus { outline: none }`). Karten-Track mit `data-lenis-prevent-horizontal`: horizontale Wheel-/Trackpad-Gesten bleiben nativ (Scroll-Snap), vertikale gehen an Lenis – geprüft.
- Abnahme (neue Skripte `scripts/motion-check.mjs` und `scripts/scroll-perf.mjs`; `page-shot`/`browser-eval` mit `--no-js` / `--reduced-motion`): (1) JS aus, 1920: `docs/screens/phase-7a-1920-nojs.png` – alle Inhalte sichtbar, 24 Motion-Ziele mit Opazität 1 ohne Transform; (2) `prefers-reduced-motion: reduce`: `phase-7a-1920-reduced.png` – kein Lenis, keine Tweens, zwei Viewport-Captures im Abstand von 1,5 s am Marquee byte-identisch, Nav-Zustand funktioniert weiterhin; (3) Performance über den kompletten Scroll (Wheel 120 px / 30 ms, 6167 px): **0 Long Tasks**, längste Renderer-Task im Trace 6 ms (`docs/screens/phase-7a-scroll-trace.json`, in den DevTools ladbar), CLS 0,0008; Mobile-Emulation 390 (8718 px): 0 Long Tasks, CLS 0. Funktionstests 24/24: Lenis aktiv, Nav-Zustand, `aria-current`, Anker bei 96 px mit Hash + Fokus, Reveals, Marquee läuft, Hero-Parallax scrubbt (scale 1,049 bei 480 px), CTA-Puls, Karten-Track horizontal nativ, Glow folgt dem Pointer; 390: Lenis aktiv, Hero ohne Scale (y 30 bei 390 px), kein Overflow. Lighthouse (`check:phase 7a`): Desktop a11y **100** / BP **100** / CLS 0,000, Mobile **100** / **100** / 0,001.
- Beobachtung (nicht Teil der Scroll-DoD): beim Laden eine Task von ≈ 50–62 ms (HTML-Parse + erstes Layout; per Trace ohne JS 38 ms, mit JS 47 ms → Init von animations.ts ≈ 10 ms). Für Phase 8 (TBT) im Blick behalten.
- Offen für Teil B: DoD-Punkte „alle 15", „Mobile echtes Gerät" (Touch nur emuliert; Lenis ist auf Touch per `syncTouch: false` inaktiv) und der Sammel-Commit `feat(phase-7)`; `HexCursor.astro`/`hex-cursor.ts` noch nicht angelegt.
- Gelernt (Tooling): mit `Emulation.setScriptExecutionDisabled` läuft `Runtime.evaluate` weiter, aber Timer aus der Seite feuern nie – page-shot wartet jetzt Node-seitig. Lenis setzt `lenis-smooth` nur während der eigenen Animation, dazwischen gilt `scroll-behavior: smooth` der Seite (zwei native Sprünge nacheinander heben sich im Harness auf → `scrollBehavior = 'auto'` im Test). Ein einzelner Wheel-Schritt von 300 px snappt auf dem Karten-Track zurück auf Karte 1 (mandatory snap), ab 400 px scrollt er.

### Phase 7 – Animationen, Teil B (Signature-Animationen) · 2026-09-09 · Commit `f21c7c0`

- Umfang Teil B (Auftrag): Punkte 1, 7, 9, 10, 15 – Punkt 9 damit zugeordnet und umgesetzt. Vorab die DoD von Phase 6 entschärft: „±8 px bei Höhen" → „strukturell 1:1, Höhenabweichung durch Font-Metriken dokumentiert" (`docs/phases/PHASE-6-responsive.md`).
- **Referenzvideos:** `_material/referenzen/` existiert nicht (in `_material/` liegen nur Bilder, Fonts, Logos, Figma-Export, Beteiligungstexte). Hero-Typo und Beteiligungs-Sequenz daher nach der Beschreibung im Phasen-Doc gebaut. Angestrebte Bewegung: Beim Laden steigen die Buchstaben von „Build" und „Beyond." nacheinander ein Stück von unten auf und werden dabei sichtbar, danach folgen Eyebrow, Text und Buttons; beim Scrollen wandert „Beyond." Buchstabe für Buchstabe nach rechts auseinander (das Tracking wächst), während der ganze Textblock nach oben zieht und ausblendet. Beim Beteiligungswechsel blendet das alte Panel kurz aus, das neue blendet ein, sein Foto zoomt von 1,05 auf 1 zurück und Logo, Text, Chips und Button rutschen leicht versetzt von rechts ein. Begründung der Umsetzung: ohne Referenz gelten die Parameter aus dem Phasen-Doc (Stagger 0,03, Scrub 0,6, `y −80`, 0,25 / 0,6 s, `x 24`, Stagger 0,06); die Buchstaben steigen ohne Masken-Clip, weil `overflow: hidden` je Zeile bei Zeilenhöhe 1,0 die Unterlängen („y") abschneiden würde; das Intro wartet auf `document.fonts.ready` (Heavy ist nicht vorgeladen), damit keine Glyphen mitten in der Bewegung wechseln. Sobald die Videos vorliegen, Feinabstimmung in `animations.ts` (`initHero`, `initSpotlight`) – alle Dauern, Distanzen und Stagger stehen dort als Literale.
- 1 Hero-Typo: `Hero.astro` splittet die Headline zur Buildzeit in `<span class="char" data-hero-char>` (inline-block, `white-space: nowrap` je Zeile, `data-hero-line="solid|outline"`); Screenreader lesen eine `visually-hidden`-Kopie „Build Beyond.", die Buchstaben-Spans sind `aria-hidden`. Intro-Timeline (pausiert, `play()` nach `document.fonts.ready`, spätestens nach 1 s): Buchstaben `yPercent 60 → 0, opacity 0 → 1`, 0,8 s `power4.out`, Stagger 0,03; Eyebrow/Text/Buttons (`data-hero-fade`) ab 0,35 s `y 24 → 0` (mobil 16), Stagger 0,1. Scroll (`scrub: 0.6`, Hero `top top → bottom top`): Outline-Buchstaben `x = Index × 6 px` (mobil 3), Textblock (`data-hero-content`) `y → −80, opacity → 0` (mobil −40). Nebenwirkung des Splits: kein Kerning zwischen Inline-Blocks – „Beyond." 547 statt 542 px bei 120 px (mobil 310 statt 307), „Build" unverändert 296 px; bei 10 px Tracking nicht sichtbar.
- 7 Warum-K+-Slider (native Variante, **kein Pinning**): Karten `y 24 (mobil 16), opacity 0` per `ScrollTrigger.batch` mit Stagger 0,12 (Desktop: alle sechs auf einmal, mobil je Batch beim Hineinscrollen – bei 390 zuerst zwei, dann alle). Fortschrittslinie: das Inline-Script in `WarumKplus.astro` bleibt die Quelle (setzt `--progress` und den Zähler) und meldet den Füllwert als `warum:progress`; `animations.ts` zieht `scaleX` per `gsap.quickTo` (0,4 s, `power3`) nach. Die CSS-Transition an der Linie ist raus (sie liefe gegen GSAP; bei reduced motion springt die Linie, Zähler und Snap bleiben nativ).
- 9 Logo-Wall: Tiles `y 24, opacity 0`, Stagger 0,08 beim Eintritt (`top 85%`, once). Hover-Lift jetzt in der CSS-Eigenschaft `translate` (GSAP besitzt `transform` allein), Rahmen, Glow, Füllung und Name wechseln in 300 ms (`--dur-base`), der Lift bleibt 200 ms.
- 10 Spotlight-Wechsel: `portfolio.ts` feuert `portfolio:change` jetzt **cancelable und vor** dem Umschalten; ohne Abbruch setzt es `hidden` wie bisher (kein JS-Motion, reduced motion). `animations.ts` bricht ab und übernimmt: altes Panel `opacity → 0` (0,25 s `power1.out`, danach `hidden`), neues Panel ab 0,1 s `opacity 0 → 1`, Bild (`data-spot-media` = `<picture>`) `scale 1.05 → 1`, Textspalte (`data-spot-text > *`) `x 24 → 0` Stagger 0,06, alles 0,6 s `power4.out`; am Ende `clearProps`. Schnelle Klicks: laufende Timeline wird gekillt, alle Panels außer dem alten und neuen sofort `hidden`. Layout: `.spotlights { display: grid; grid-template-columns: minmax(0, 1fr) }`, alle Panels `grid-area: 1 / 1` – während der Überblendung liegen zwei Panels in derselben Zelle, die Höhe bleibt (Desktop alle 480 px).
- 15 Hex-Cursor: neu `HexCursor.astro` (Canvas `position: fixed`, `z-index: 50` unter der Nav, `pointer-events: none`, `mix-blend-mode: screen`, im HTML `hidden`, in `Base.astro` nach dem Footer) und `src/scripts/hex-cursor.ts` (eigener Chunk 1,0 kB gzip, per `import()` nach `load` geladen – nur im gsap.matchMedia-Kontext `(pointer: fine) and (prefers-reduced-motion: no-preference)`, bei Wechsel wird es zerstört und das Canvas wieder `hidden`). Pointy-Top-Hexagone mit Kante 28 px, 1 px Linien magenta, nur Zellen im Umkreis werden gezeichnet (ein Pfad, ein Stroke), dann radialer Alpha-Verlauf (1 → 0,55 bei 55 % → 0 bei 220 px) per `destination-in`; Position `lerp 0.15`; rAF läuft nur, solange die Maus in den letzten 2 s bewegt wurde und der Tab sichtbar ist; `mouseleave` am `<html>` löscht das Canvas; `resize` setzt die Canvas-Größe neu (DPR max. 2). Über dem Hero-Foto ist das Raster durch den Screen-Blend deutlich dezenter als über Night (`docs/screens/phase-7b-hex-hero-1920.png`, `phase-7b-hex-warum-1920.png`).
- Abnahme (`motion-check.mjs` auf 43 Checks erweitert; `scroll-perf.mjs`, `page-shot.mjs`, Screenshots per CDP-Skript): (1) JS aus, 1920: `phase-7b-1920-nojs.png` – 52 Motion-Ziele sichtbar und ohne Transform, Canvas `hidden`, `h1` liest „Build Beyond."; (2) reduced motion: `phase-7b-1920-reduced.png` – 52 Ziele, kein Lenis, Marquee-Captures identisch, kein Canvas, Spotlight-Wechsel sofort (genau ein Panel); (3) Scroll-Trace 1920 (`phase-7b-scroll-trace.json`, Wheel 120 px / 30 ms über 6167 px): **0 Long Tasks**, längste Renderer-Task 11 ms, CLS 0,0009; 390 (8718 px): 0 Long Tasks, CLS 0,0000; Load-Task unverändert ≈ 54–68 ms; (4) JS 1920: Hero-Intro nach 2,5 s gesetzt (12 Buchstaben, 3 Fades), bei 480 px Scroll Textblock Opazität 0,49 / y −41 und letzter Outline-Buchstabe x 18,5; Karten sichtbar, Fortschrittslinie scaleX 0,5 bei Zähler 03/06 nach zwei 400-px-Wheel-Schritten; Tiles gesetzt; **Spotlight-Wechsel per CDP** (drittes Tile geklickt): nach 120 ms zwei Panels sichtbar (Opazität 0,22 / 0,25), Höhe 480,2 → 480,2; nach 1,1 s genau ein Panel (Index 2, Opazität 1, Bild und Textspalte ohne Transform, `aria-selected` 2), Höhe 480,0 (Δ 0,2 px = Kappes-Panel läuft 0,17 px über `min-height`); zwei Klicks im Abstand von 80 ms → genau ein Panel (Index 4); Hex-Canvas bei `pointer: fine` sichtbar, 273 gezeichnete Pixel um den Mauspunkt, 0 in 400 px Entfernung, `z-index` 50 < Nav 100, `pointer-events: none`, Blend `screen`. 390 (Emulation, `pointer: fine` bleibt dort wahr – auf echten Touch-Geräten greift `coarse` und das Script wird nicht geladen): Hero bei 300 px Scroll Opazität 0,62 / y −15 / Drift 6,9, Karten batchweise (2, dann 6), Spotlight 760 → 592 px (unter 1024 px sind die Panels inhaltshoch, die Höhe wechselt erst, wenn das alte Panel nach 0,25 s `hidden` wird – wie vor Phase 7 beim Sofortwechsel), kein Overflow. Sicht-Screenshots `phase-7b-hero-intro-1920.png`, `phase-7b-hero-scroll-1920.png`, `phase-7b-spot-midfade-1920.png`, `phase-7b-spot-after-1920.png`. Lighthouse (`check:phase 7b`): Desktop a11y **100** / BP **100** / CLS 0,000, Mobile **100** / **100** / 0,001. Bundle: Hauptmodul **51,6 kB gzip** (+1,4 kB gegenüber Teil A), Hex-Chunk 1,0 kB.
- Offen: Test auf einem echten Mobilgerät (Touch); Feinabstimmung von Hero-Intro und Beteiligungs-Sequenz nach Sichtung bzw. sobald die Referenzvideos in `_material/referenzen/` liegen; Pinning-Variante für den Slider bewusst nicht gebaut.
- Gelernt (Tooling): `img.decode()` an einem Lazy-Bild, dessen Anfrage noch nicht gestartet ist, löst nie auf – vor dem Decode `loading = 'eager'` setzen (page-shot macht das schon, das CDP-Screenshot-Skript hing daran 150 s). `clearProps: 'all'` würde auch die Inline-Custom-Properties der Logo-Masken (`--logo`) löschen – nur `transform,opacity` löschen. CSS-Transitions auf `transform` (Tile-Hover, Fortschrittslinie) laufen gegen GSAP: Hover-Lift in `translate`, Linie ohne Transition.

### Phase 7 – Animationen, Teil B/2 (Hero „Push-through") · 2026-09-10 · Commit `c8d0c5c`

- Auftrag: Punkt 1 neu bauen, Referenz Codegrid/Jesko Jets „This Hero Scroll Animation Feels Like Looking Through a Window" – gepinnte Hero-Sektion, eine Scrub-Timeline. Die bisherige Fassung (Outline-Drift + Textblock nach oben ausblenden, Teil B) ist damit ersetzt; **Punkt 2 (Bild-Parallax) ist in dieselbe Timeline gewandert**, es gibt keinen zweiten Hero-Trigger mehr.
- **Layout (Spec-Abschnitt 1, bewusste Abweichung 9).** Ab 768 px steht die Headline einzeilig zentriert („Build Beyond.") statt zweizeilig links gestapelt; der ganze Textblock ist zentriert. Startgröße füllt den Container: `--fs-display: calc(100cqw / 7.4)` über einen Container-Query auf `.hero__content` – **216 px bei 1920**, Zeilenbreite 1544 von 1600 (je 28 px Luft), 151 px bei 1440, 121 px bei 1024, 93 px bei 768; oberhalb 1920 hält der 1600er Container die Größe. Der Divisor 7.4 ist die gemessene Zeilenbreite in em (7,09 bei Tracking 0.08em) plus ~4 % Luft. Die Wörter stehen mit `margin-left: 0.215em` (gemessene Leerzeichenbreite der KMR Heavy) statt mit einem Textknoten nebeneinander, damit sie unter 768 px per `display: block` stapeln können; die nachlaufende Sperrung des letzten Outline-Glyphen wird per negativem `margin-right` abgezogen (Zeile sitzt links wie rechts exakt 188 px vom Rand). Hero-Höhe ab 768 px `100svh` statt `min(936px, 100svh)` – ohne vollen Viewport schaut während des Pins Vision unter dem Hero hervor. Body-Text: je Satz ein `<span class="hero__text-line">` statt `<br>` (Staffel-Einheiten), Breite `min(809px, 100cqw / 1.5)`, damit der Block auch vergrößert in den Container passt. **Mobile-Frame `242:22` bleibt unangetastet** (zweizeilig, linksbündig, 78 px, kein Tracking) – einzeilig wären dort nur 46 px drin.
- **Timeline** (`initHero`, normiert auf 1, Pin-Länge 1,5 Viewport-Höhen, `scrub: 0.6`, `anticipatePin: 1`, `invalidateOnRefresh`): `0 → 0.72` Headline `scale 1 → 8` aus der Mitte (`power2.in` – in GSAP kubisch, nicht quadratisch), Bild `scale 1 → 1.12` mit derselben Kurve, Overlay (night) `opacity 0 → 1`, voll bei 0.65; `0 → 0.24` Eyebrow und Body-Zeilen `y −60, opacity → 0` gestaffelt (0.043); `0.54 → 0.72` Headline `opacity → 0`; Label `through` bei 0.72; `0.72 → 1` Body-Block `scale 1.5` (aus `--hero-reveal-scale`, `transform-origin` Unterkante, damit der 40-px-Abstand zu den Buttons bei jeder Breite und Textlänge hält) und Zeilen aus `y 90, opacity 0` gestaffelt herein. Die Buttons bekommen **keinen** Tween und liegen per `z-index` über der Headline – gemessen: `matrix(1, 0, 0, 1, 0, 0)`, Opazität 1 an jeder Timeline-Position. Der Reveal nutzt `immediateRender: false`, sonst würde GSAP die From-Werte schon bei Timeline-Bau anwenden und der Ruhezustand wäre leer.
- **Masken-Ebene später ohne Umbau:** die Skalierung läuft auf *allen* `[data-hero-push]`-Elementen in einem Tween (heute nur der Typo-Wrapper). Eine Portal-/Masken-Ebene bekommt dasselbe Attribut und läuft mit; das Ausblenden der Type hängt getrennt an `[data-hero-type]` und kann dann entfallen. Konstanten (`PUSH_SCALE`, `PUSH_EASE`, `PUSH_THROUGH`, `PUSH_DISTANCE`) stehen oben in `animations.ts`.
- **Unter 768 px** (`gsap.matchMedia`-Bedingung `pinned: '(min-width: 768px)'`, neben `desktop: 1024`): kein Pin, Trigger `top top → bottom top`, nur `scale 1 → 2.6` und Fade, kein Reveal (`--hero-reveal-scale: 1`). Geprüft bei 390: kein `.pin-spacer`, Hero 780 px, Headline 78 px zweizeilig, bei 60 % Scroll Typo `scale 1.35`, Overlay 0,67.
- **Ohne JS / reduced motion:** Ruhezustand. Das Night-Overlay hat seinen Ruhewert `opacity: 0` im CSS (dekorative Ebene, kein Inhalt) – motion-check prüft das jetzt explizit, dazu „kein Pin-Spacer" in beiden statischen Modi.
- **Zwei Fehler dabei gefunden und behoben** (beide außerhalb des Auftrags, aber im selben Modul): (1) `initNav` nahm `section.nextElementSibling` als `endTrigger` – zwischen den Sektionen stehen Astros Inline-Scripts, also war der `endTrigger` von `#warum-kplus` und `#portfolio` ein `<script>` ohne Box und der aktive Nav-Link quer über die Seite falsch (Bestand seit Teil A); jetzt wird bis zur nächsten `SECTION` weitergelaufen. (2) Der neue Pin verlängert die Seite um 1,5 Viewports, die Nav-Trigger werden aber vor ihm angelegt und lagen dadurch exakt 1620 px zu tief – `refreshPriority: 1` am Hero-Trigger lässt ihn zuerst messen. Nachgemessen über die ganze Seite: `#warum-kplus` / `#portfolio` / `#schwerpunkte` schalten jetzt an den richtigen Stellen.
- **Abnahme.** `motion-check.mjs` auf **54 Checks** erweitert (Hero-Checks ersetzt: Pin hält bei `top 0`, Typo skaliert 1,49 bei 480 px Scroll, Bild 1,008, Overlay 0,457, Eyebrow/Zeilen weg, Buttons unangetastet; am Pin-Ende Body `scale 1.5`, Zeilen bei `y 0` und Opazität 1, Type `opacity 0`) – **alle grün**. Timeline-Screenshots bei 0 / 0.5 / 1: `docs/screens/phase-7b-hero-p0.png`, `-p05.png`, `-p1.png` (1920, gescrollt auf 0 / 810 / 1620 px), dazu `phase-7b-hero-nojs-1920.png` und `phase-7b-hero-390.png`. Scroll-Trace **über die gepinnte Sektion** (`--max=1700 --step=60`, `docs/screens/phase-7b-hero-pin-trace.json`): **0 Long Tasks**, längste Renderer-Task 4 ms, CLS 0,0004. Ganze Seite 1920 (`phase-7b-scroll-trace.json`, 7931 px): 0 Long Tasks, längste Task 7 ms, CLS 0,0009; 390 (8718 px): 0 Long Tasks, CLS 0,0000. Lighthouse (`check:phase 7b`): Desktop a11y **100** / BP **100** / CLS 0,000, Mobile **100** / **100** / 0,001. Bundle: Hauptmodul **50,7 kB gzip** (−0,9 gegenüber Teil B, der Drift-Code ist weg), Hex-Chunk 0,9 kB.
- **Harness:** `scroll-perf.mjs` bekommt `--max=<px>`, um einen Abschnitt dicht abzutasten statt die ganze Seite (das Ziel der Laufweite geht in die Bewertung ein). In `motion-check.mjs` sprang der Nav-Check bisher mit `scrollIntoView` + `scrollBy` – von zwei nativen Scrolls im selben Task behält Lenis nur einen, der Test maß also die Position des vorherigen Schritts; jetzt ein absoluter `scrollTo`.
- **Offen:** Masken-/Portal-Ebene (bewusst nicht in diesem Schritt); Test auf einem echten Mobilgerät; die Typo skaliert bei `scale 8` sichtbar an der Rasterung von Chrome vorbei – kein `will-change` gesetzt, damit die Schrift an den Haltepunkten scharf bleibt (bei 0.5 und 1 im Screenshot geprüft).

### Phase 7 – Animationen, Teil B/3 (Hero: Copy erscheint nach dem Durchflug) · 2026-09-10 · Commit `892fa3d`

- Auftrag: Eyebrow, Body und beide Buttons sind beim Laden nicht sichtbar und erscheinen erst nach dem Durchflug; dazu ein dezenter Scroll-Hinweis, weil sonst nur die Headline zu sehen ist.
- **Intro-Timeline** hat jetzt nur noch die Headline-Buchstaben (`yPercent 60 → 0`, Stagger 0.03, 0,8 s `power4.out`). `data-hero-fade` ist ersatzlos raus, `data-hero-out` heißt `data-hero-eyebrow`.
- **Verstecken** per `gsap.set([eyebrow, text, actions], { opacity: 0 })` – nur im JS und nur bei `pinned === true`. Ohne JS und bei reduced motion ist der Hero vollständig; unter 768 px ebenfalls, dort gibt es keinen Durchflug. `gsap.matchMedia` nimmt das Set beim Wechsel unter 768 px selbst zurück.
- **Reveal am Label `through`** (0.72 → 1, Restspanne 0,28): drei Gruppen à `enter` = 0,112, um `gap` = 0,084 versetzt, dadurch landen die Buttons exakt bei 1. Reihenfolge Eyebrow → Textzeilen (Stagger innerhalb der Gruppe) → Buttons, alle mit `y 40 → 0` (unter 1024: 28) und `power4.out`. Nur die Copy skaliert (`--hero-reveal-scale` 1,5), Eyebrow und Buttons kommen in normaler Größe. Alle Tweens `immediateRender: false`, sonst zöge GSAP die From-Werte schon beim Timeline-Bau an und der Ruhezustand wäre leer.
- **Eyebrow-Landepunkt gemessen statt Layout-Platz.** Die dissolvierte Headline behält ihre Box (216 px bei 1920), der Eyebrow stünde also fast 300 px über der Copy – im ersten Durchlauf sah das Endbild aus wie ein Fehler. Er landet jetzt einen Gap über der **skalierten** Oberkante der Copy: `text.offsetTop − Texthöhe × (scale − 1) − rowGap − Eyebrow-Unterkante` (bei 1920 = 214 px), als Funktionswert, damit `invalidateOnRefresh` neu misst. Beim ersten Versuch stand dort `× (scale − 1) / 2` – falsch, weil `transform-origin` der Copy auf der Unterkante liegt und sie deshalb um die volle Höhendifferenz nach oben wächst (gemessener Abstand 19 statt 40 px).
- **Scroll-Hinweis** (neu `hero.scrollHint` = „Scrollen" in `content/de.json`, `[data-hero-hint]`): unten zentriert, 40 px über der Kante, 14 px Medium uppercase mit `--ls-hint` (2 px, neuer Token) in `--c-muted`, darüber eine 1 px / 48 px hohe Linie im Verlauf transparent → muted. `aria-hidden`, `pointer-events: none`, zentriert über die CSS-Eigenschaft `translate` (GSAP besitzt `transform`). Blendet über die ersten 15 % der Timeline aus. Unter 768 px `display: none`.
- **Vereinfacht:** die Buttons haben keine Sonderrolle mehr – `position: relative; z-index: 1` an `.hero__actions` ist raus, sie sind während des Durchflugs ohnehin unsichtbar.
- **Abnahme.** `motion-check.mjs` auf **57 Checks** (Hero-Checks umgeschrieben: beim Laden Buchstaben gesetzt, die drei Blöcke bei Opazität 0, Hinweis bei 1; bei 480 px Scroll Hinweis 0 und die drei weiter 0; am Pin-Ende alle drei bei 1, Copy `scale 1.5` gegen `--hero-reveal-scale`, Eyebrow und Buttons `scale 1`, Zeilen bei `y 0`, Abstand Eyebrow → Copy 40 px und Reihenfolge von oben nach unten) – **alle grün**, statische Modi 56 Ziele. Timeline-Screenshots `docs/screens/phase-7b-hero-p0.png` (nur Headline + „SCROLLEN"), `-p05.png`, `-p1.png` (Eyebrow, Text, beide Buttons), dazu `phase-7b-hero-nojs-1920.png` (ohne JS alles sichtbar) und `phase-7b-hero-390.png`. Scroll-Trace über die gepinnte Sektion (`--max=1700 --step=60`): **0 Long Tasks**, längste Renderer-Task 17 ms, CLS 0,0004. Ganze Seite 1920: 0 Long Tasks, 9 ms, CLS 0,0008; 390: 0 / 0,0000. Lighthouse (`check:phase 7b`): Desktop a11y **100** / BP **100** / CLS 0,000, Mobile **100** / **100** / 0,001. Bundle 50,8 kB gzip.
- **Offen:** unverändert – Masken-/Portal-Ebene, Test auf einem echten Mobilgerät.
### Phase 7 – Animationen, Teil B/4 (Hero: gleichmäßiges Overlay, Hintergrundvideo) · 2026-09-10 · Commit `9ad2db2`

- Auftrag: (1) die sichtbare horizontale Verlaufskante im Hero-Banner beseitigen – `.hero::before/::after` ersatzlos, stattdessen ein gleichmäßiges Overlay mit Grundopazität in CSS, Timeline von dort auf 1; (2) ein Hintergrundvideo als unterste Ebene, nur zur lokalen Beurteilung (watermarked Preview, Lizenz offen).
- **Kante gefunden und erklärt.** Die beiden Verläufe waren je 50 % hoch und trafen sich in der Bildmitte mit **0.301 (oben, `--c-night-a30`) gegen 0.401 (unten, `--c-night-a40`)** – dieser Sprung war die Kante, und er lag genau in der Mitte der Headline-Box (bei 1080 px Viewport y 393–609, Mitte 540). Über die Box lief die alte Alpha 0.49 → 0.30 → 0.40 → 0.48. Beide Pseudoelemente sind raus, die Tokens `--c-night-a30`/`-a40` damit auch (sonst tote Werte mit falschem Kommentar).
- **Neuer Ruhewert gemessen, nicht geschätzt: `--hero-overlay-rest: 0.45`** (lokale Custom Property an `.hero`, Vorgabe war „etwa 0.35"). Gemessen gegen den hellsten 8×8-Fleck in der Headline-Box über den kompletten Video-Loop (acht Timecodes, Kompositing in sRGB): bei 0.35 fällt die Lime-Outline auf **2,90:1** und liegt unter der 3:1-Schwelle für große Schrift; bei 0.45 stehen Weiß bei **≥ 4,44:1** und Lime bei **≥ 3,78:1**. 0.45 liegt zudem mitten im alten Bereich (0.30–0.49), die Banner-Dichte bleibt also erhalten. Sichtvergleich 0.35 / 0.45 / 0.5 gemacht – bei 0.5 gewinnt die Lesbarkeit kaum noch, das Material verliert aber Zeichnung.
- **Timeline unverändert im Aufbau:** `timeline.to(overlay, { opacity: 1 })` startet ohnehin beim aktuellen Wert, also jetzt bei 0.45 statt 0. Position 0 der Timeline ist damit **exakt** der Zustand ohne JS.
- **Nebenwirkung (offen, außerhalb des Auftrags):** der obere Verlauf lief am Frame-Rand in volles night und war zugleich der Hintergrund der Nav. Die Teal-Links stehen am Seitenanfang jetzt bei **3,9–4,2:1** statt ≈ 7:1 (ab 40 px Scroll liegt `--c-night-a85` hinter der Nav, dort unverändert). Kein Lighthouse-Befund (axe rechnet Text über Bildern nicht), aber sichtbar schwächer. Falls das zu wenig ist: eigener Nav-Scrim, **nicht** ein zweiter Verlauf am Bild.
- **Video-Ebene.** `_material/hero-video-preview.mp4` (898×506, 10,8 s, H.264 + AAC, 1,0 MB) nach `public/video/hero.mp4` kopiert – **vorher** `public/video/` in `.gitignore` (mit `git check-ignore -v` geprüft, bevor die Datei existierte). Ebenen im Hero von unten nach oben: `<Picture>` (`z-index: -2`, bleibt `loading="eager"` + `fetchpriority="high"`, LCP-Element und **Poster-Ersatz** – ein `poster`-Attribut wäre ein zweiter Download desselben Bildes und könnte selbst LCP werden), `<video>` (`z-index: -1`), Overlay (0), Typo (1). Attribute wie beauftragt: `autoplay muted loop playsinline preload="metadata"` plus `disablepictureinpicture` und `disableRemotePlayback` – die beiden Schreibweisen sind das, was Astros `VideoHTMLAttributes` akzeptieren (die eine nur klein, die andere camelCase); im HTML landen ohnehin beide als Attribut.
- **Kein `src` im Markup.** Bei `preload="metadata"` würde die Datei sonst ohne JS angefragt. Der Pfad steht als `data-src` (Wert aus `de.json`, neu `hero.video.src`); neu `src/scripts/hero-video.ts` (803 Byte, von Astro **inline** ins HTML gelegt – kein zusätzlicher Request) setzt ihn per IntersectionObserver am `[data-hero]`, und nur wenn `(min-width: 768px)` und **nicht** `prefers-reduced-motion: reduce`; abgespielt wird erst nach `canplay`, `.is-playing` blendet dann über das Bild (`--dur-base`; global.css schaltet Transitions bei reduced motion ab). Ruhezustand im CSS `opacity: 0`, damit ohne JS keine schwarze Fläche über dem Bild liegt. Zusätzlich: Pause beim Verlassen des Viewports und bei `document.hidden`; `matchMedia`-Listener, damit ein Viewport, der über 768 px wächst, das Video noch bekommt und reduced motion es wieder abschaltet.
- **`data-parallax` trägt jetzt auch das Video**, und `initHero` nimmt `querySelectorAll` statt `querySelector` – Bild und Video laufen in **einem** Tween (`scale 1 → 1.12`), können also nicht auseinanderdriften; ohne Video skaliert weiter nur das Bild (mobil, reduced motion).
- **Abnahme.** `motion-check.mjs` erweitert (Overlay gegen `--hero-overlay-rest` statt gegen 0; Video ohne `src`, `networkState 0`, Opazität 0 in beiden statischen Modi; **keine Videodatei angefragt** – `Network.requestWillBeSent` je Modus mitgeschrieben; mit JS: läuft nach `canplay`, stumm, Loop, `.is-playing` bei Opazität 1, nur `/video/hero.mp4` angefragt, Video skaliert exakt wie das Bild; unter 768 px: kein `src`, keine Anfrage) – **64 Checks, alle grün**. Das Video ist aus der Sammelprüfung „alle Motion-Ziele sichtbar" ausgenommen (`[data-parallax]:not([data-hero-video])`), es ist absichtlich transparent. Timeline-Screenshots `docs/screens/phase-7b-hero-p0.png` (Overlay 0.45, Video läuft, keine Kante), `-p05.png` (0,8744), `-p1.png` (1,0 – volles night, Copy da); statisch `phase-7b-hero-nojs-1920.png` und `phase-7b-hero-reduced-1920.png` (Foto sichtbar, alle Inhalte da, keine schwarze Fläche), `phase-7b-hero-390.png`. Scroll-Trace über die gepinnte Sektion (`--max=1700 --step=60`, `phase-7b-hero-pin-trace.json`): **0 Long Tasks**, längste Renderer-Task 5 ms, CLS 0,0005. Ganze Seite 1920 (`phase-7b-scroll-trace.json`, 7931 px): 0 Long Tasks, 12 ms, CLS 0,0010; 390 (8718 px): 0 Long Tasks, CLS 0,0000. Lighthouse (`check:phase 7b`): Desktop a11y **100** / BP **100** / CLS 0,000, Mobile **100** / **100** / 0,001. Hauptmodul unverändert **52,08 kB gzip** (52 077 → 52 079 Byte, im selben Lauf gemessen), Hex-Chunk 1,0 kB. `git status` führt weder `public/video/` noch die Datei.
- Bei 390 fallen im `js`-Modus sieben Checks – dieselben sieben wie **vor** dieser Änderung (per `git stash` gegengeprüft): sie prüfen den gepinnten Desktop-Hero (Pin, verstecktes Copy, Eyebrow-Abstand) sowie Spotlight-Höhe und Karten-Track, die unter 768 px anders funktionieren. Kein neuer Befund; die Video- und Overlay-Checks sind dort grün.
- **Load-Task:** 74–75 ms (vorher ≈ 54–68 ms) – die Videoebene kostet beim ersten Layout etwas. Für Phase 8 (TBT) im Blick behalten, wie schon notiert.
- **Offen:** lizenzierte Fassung des Videos (Zielformat in `docs/ASSETS.md`: 1920 breit, ohne Tonspur, H.264-MP4 + WebM, < 3 MB); Entscheidung, ob die finale Datei ins Repo oder außerhalb ausgerollt wird; Nav-Kontrast am Seitenanfang (siehe oben); unverändert Masken-/Portal-Ebene und Test auf einem echten Mobilgerät.
### Phase 7 – Animationen, Teil B/5 (Hero-Feinschliff: Scrub, Schärfe, Kante, Logo) · 2026-09-10 · Commit `b81039d`

- Auftrag: vier Punkte am Hero – (1) ruckelndes Nachlaufen der Copy-Reveal, (2) dauerhaft unscharfe Copy auf `scale(1.5)`, (3) Videostreifen an der Hero-Unterkante, (4) Logo soll beim Verlassen des Hero scroll-gekoppelt schrumpfen.

**1 Scrub / Nachlaufen – gemessen, Diagnose teilweise korrigiert.** Neu `scripts/scroll-smoothness.mjs`: CDP-Screencast (JPEG q100, Frame-zu-Frame-Differenz einer Region) **plus** rAF-Recorder in der Seite, der pro Frame Scrollposition und Sondenposition mitschreibt. Getrennt ausgewertet werden „Lag" (Frames, in denen sich die Sonde noch bewegt, obwohl der Scroll längst steht = zweite Glättung) und „Sub-Pixel-Lauf" (Strecke, auf der sich noch etwas unter 1,9 px pro Frame bewegt). Messpunkt für die Copy ist `[data-hero-text-line]` – der Container bewegt sich nicht, die Zeilen tragen das `y`.

| Variante | Sonde steht nach | Sub-Pixel-Lauf | Lag |
|---|---|---|---|
| `scrub: 0.6`, Sonde mitten im Pin (`--to=1400`) | 327 ms (10 Frames) | 5 Frames | 0 |
| `scrub: true`, dito | **196 ms (6 Frames)** | **2 Frames** | 0 |
| `scrub: 0.6` am Pin-Ende (`--to=1620`) | 836 ms | 30 Frames | 0 |
| `scrub: true` am Pin-Ende | 806 ms | 28 Frames | 0 |

- Umgesetzt: `PUSH_SCRUB = true` (Konstante oben in `animations.ts`, mit Begründung). Innerhalb des Pins ist das messbar besser – die Timeline folgt der Scrollposition direkt statt mit eigener 0,6-s-Nachführung; der Sub-Pixel-Lauf der Copy geht von 5 auf 2 Frames, das Einschwingen von 327 auf 196 ms. `0.2` wurde nicht gebraucht, weil `true` sauber lief.
- **Die Hauptursache ist aber nicht der Scrub, sondern Lenis.** Am Pin-Ende sind beide Varianten praktisch gleich, und die Sondenschritte sind dort **identisch mit den Scrollschritten** (Lag 0): `scrollY` selbst läuft nach dem Loslassen ~800 ms in **1/0-Schritten** aus. Grund: `damp(value, to, lerp*60, dt)` ist eine Exponentialglättung, und Lenis beendet sie erst, wenn `Math.round(value) === Math.round(to)` – die letzten ~20 px werden mit unter 1 px pro Frame abgebaut, und der Dokument-Scroll wird auf ganze Gerätepixel gerundet. Das erklärt auch das gefilmte „dann Sprung": Lenis setzt am Ende die restlichen < 0,5 px in einem Frame. Gemessen am Pin-Ende: **lerp 0.1 → 806 ms / 28 Frames · lerp 0.2 → 341 ms / 10 Frames · lerp 0.3 → 234 ms / 5 Frames** (erst 0.3 liegt unter der Schwelle von 6 Frames). Lenis steht weiter auf **0.1** – der Wert ist in Teil A entschieden und gilt für die ganze Seite, nicht nur den Hero; die Entscheidung liegt beim Kunden/Michael (**offen**, eine Zeile in `initLenis`).
- **Video-A/B (Auftrag):** mit Video 732 ms / 22 Frames, mit blockierter Videodatei (`Network.setBlockedURLs`, Flag `--no-video`) 752 ms / 26 Frames. Das dekodierende Hintergrundvideo trägt **nichts** zum Nachlaufen bei (die Differenz liegt in der Streuung, der Lauf ohne Video war minimal schlechter).

**2 Schärfe.** `--hero-reveal-scale` heißt jetzt `--hero-copy-scale` und ist kein Transform mehr, sondern ein Größenfaktor: `.hero__text` bekommt `font-size: calc(var(--fs-body) * var(--hero-copy-scale))` = **27 px** (Zeilenhöhe folgt, `--lh-body` ist ein Verhältnis) und `max-width: min(809px × 1.5, 100cqw)`. Die Timeline setzt nur noch `opacity` – das frühere `scale`-Set (Dauer 0) ist raus, der Ruhezustand ist `transform: none`. Der Zeilen-Einstieg läuft aus `y 90 × Faktor` = 135 px, damit die Bewegung auf dem Schirm dieselbe Strecke hat wie vorher. Der Eyebrow-Landepunkt wird dadurch einfacher: `text.offsetTop − gap − Eyebrow-Unterkante` (der Term `× (scale − 1)` entfällt, weil die Copy nicht mehr aus ihrer Box wächst).
- **Gegenprobe am Screenshot** (Ink-Bounds des Copy-Blocks bei Timeline-Position 1, 1920): vorher 1167×276 px bei y 545–820, nachher **1167×276 px** bei y **566–841**. Größe, Breite und Zeilenumbrüche sind identisch, der Block sitzt **21 px tiefer** – die Copy beansprucht ihre Höhe jetzt im Fluss, statt per Transform in die Headline-Box hineinzuwachsen. Die inneren Abstände (40 px Eyebrow → Copy → Buttons) halten unverändert (motion-check misst 40 px). Exakt gleiche Position **und** Schärfe ginge nur mit einem negativen Rand in Höhe des Zuwachses, und den kann CSS nicht aus der eigenen Elementhöhe ableiten – **offen**, falls die 21 px stören sollen (dann Anker der Hero-Komposition auf die Copy-Unterkante).
- Nebenwirkung, bewusst: ohne JS und bei reduced motion steht die Copy jetzt ebenfalls in Endgröße (vorher 18 px, weil das `scale` nur per JS kam) – der Ruhezustand ist damit derselbe wie das Reveal-Ergebnis.

**3 Kante an der Hero-Unterkante – Ursache gemessen.** Rects von Hero, Video-, Bild- und Overlay-Ebene bei `deviceScaleFactor` 1.0 und 1.25, an sechs Scrollpositionen, plus Auslesen der letzten Gerätepixelzeilen (Scratch-Skript, Ergebnis hier):
- Bei 1920×1080 fällt **keine** Rundung an (1080 × 1.25 = 1350 exakt) – dort war nichts zu sehen. Mit einer Viewport-Höhe, die auf gebrochene Gerätepixel fällt (1033 × 1.25 = 1291,25), und **hinter dem Pin** zeigte sich der Streifen: Hero-Box `-99.999..933.001`, Overlay identisch, Video (weiter auf `scale 1.12`) `-161.979..994.981`, und genau die Gerätezeile **933 = rgb(104,100,100)** gegen night `42,50,51` ringsum – Videoinhalt, der sich mit der Scrollposition änderte (104/107/98/119). Sichtbar bei **DPR 1.0**, nicht bei 1.25.
- Ursache: die Hero-Box landet nach dem Pin auf einer **gebrochenen CSS-Position** (`.001`, aus ScrollTriggers Pin-Rechnung). Chrome rundet den `overflow: hidden`-Clip der **Compositor-Ebene des Videos** nach oben (Zeile 933 liegt drin), das ins Hero-Layer **gemalte** Overlay aber nach unten (endet bei 933) – eine Zeile Video bleibt unbedeckt.
- Behoben mit zwei Zeilen, die beide nötig sind (in beiden Richtungen gegengeprüft: jede allein lässt die Zeile offen): `inset: -1px 0` am Overlay (Box reicht über die gebrochene Kante, der Überhang wird weggeclippt) **und** `will-change: opacity` (eigene Compositor-Ebene, damit der Clip mit derselben Rundung greift wie beim Video; das Overlay animiert ohnehin nur Opazität). Dazu wie beauftragt `background-color: var(--c-night)` an `.hero` als Absicherung. Gegenprobe: Viewport-Höhen 1033 / 1080 / 1035 × DPR 1.0 und 1.25, Positionen 0 und 1700 – **alle Zeilen an der Kante night**.

**4 Logo scroll-gekoppelt.** Neue Tokens `--nav-logo-width: 174px` / `--nav-logo-width-min: 120px`; das Logo ist jetzt breitengesteuert (`width: var(--nav-logo-width); height: auto`, Quelle 481×165 → 59,7 bzw. 41,2 hoch, Seitenverhältnis bleibt) statt `height: 60px`. Neu `initNavShrink()` in `animations.ts`: **ein** gescrubbter Trigger (`scrub: true`) bewegt Balkenhöhe **und** Logobreite gemeinsam, Start = `ScrollTrigger.getById('hero-push').end` (das Pin-Ende selbst, kein zweiter Wert, der auseinanderlaufen kann), Ende +`NAV_SHRINK` = 300 px, Werte per `invalidateOnRefresh` aus den Tokens gelesen. Gemessen: Pin-Ende 60 / 174×59,7 → +150 px **68 / 147×50,4** → +300 px **76 / 120×41,2** (Screenshot `docs/screens/phase-7b-nav-shrink.png`, vier Streifen).
- Die CSS-Transition auf `height` am Balken ist **raus** – sie liefe gegen den Scrub. `.is-scrolled` behält Hintergrund, Blur und Docking (ab 40 px) und trägt zusätzlich die Logo-Minimalbreite: damit ist die Klasse der vollständige Ruhezustand für **ohne JS** (bleibt bei 60 / 174) und **reduced motion** (springt wie bisher, Transitions sind global aus). Unter 768 px keine Kopplung (`initNavShrink` kehrt früh zurück, Balken 76, Logo 36 hoch – Mobile-Frame 242:27).

**Abnahme.** `motion-check.mjs` auf **69 Checks** erweitert und alle grün: neu „Balken und Logo in Ruhegröße, keine Inline-Größen" (beide statischen Modi, breakpoint-bewusst), „Balken und Logo am Pin-Ende noch voll", „beide gleiten gemeinsam auf halber Strecke", „Balken auf Scroll-Höhe und Logo auf Minimum nach 300 px", unter 768 px „keine Nav-Kopplung"; die Copy-Prüfung ist umgeschrieben auf „Endgröße über `font-size` (27 = 18 × 1.5), kein Transform übrig". Bei 390 fallen weiterhin **genau die sieben** vorbestehenden Checks (gepinnter Desktop-Hero, Spotlight-Höhe, Karten-Track) – die fünf neuen Nav-Prüfungen sind dort breakpoint-bewusst und grün. Statisch: `phase-7b-hero-nojs-1920.png` (Copy in Endgröße, alles sichtbar), `phase-7b-hero-reduced-1920.png`, `phase-7b-hero-390.png`; Timeline `phase-7b-hero-p0/-p05/-p1.png`. Scroll-Trace über den Pin (`--max=1700 --step=60`): **0 Long Tasks**, längste Renderer-Main-Task 6 ms, CLS 0,0005. Ganze Seite 1920: 0 Long Tasks, 12 ms, CLS 0,0011; 390: 0 Long Tasks, CLS 0,0000. Load-Task 53–54 ms (Teil B/4: 74–75). Lighthouse (`check:phase 7b`): Desktop a11y **100** / BP **100** / CLS 0,000, Mobile **100** / **100** / 0,001. Hauptmodul **52,2 kB gzip** (+0,14 gegenüber B/4), Hex-Chunk 1,0 kB.

**Offen:** Lenis-`lerp` (0.1 lässt ~800 ms Sub-Pixel-Auslauf stehen, 0.3 wäre unter der Schwelle – Entscheidung über das Scrollgefühl der ganzen Seite); die 21 px, um die der Copy-Block tiefer sitzt; Nav-Kontrast am Seitenanfang (aus B/4); Masken-/Portal-Ebene; Test auf einem echten Mobilgerät; lizenzierte Fassung des Hero-Videos.

### Phase 7 – Animationen, Teil B/6 (Nav: kein Andocken) · 2026-09-14 · Commit `0ff959e`

**Auftrag:** Beim ersten Scrollschritt schob die Nav das Logo sichtbar nach oben – das soll weg.

**Ursache.** Die Zustandsklasse `.is-scrolled` (ab 40 px) setzte den Balken von `top: 20px` auf `top: 0` und der `top`-Übergang (300 ms) ließ Logo, Links und CTA 20 px nach oben gleiten (Spec Abschnitt 0 „top 0", aus dem Figma-Zustand).

**Behoben** in `Nav.astro`: `.is-scrolled` verschiebt den Versatz von `top` in `padding-top` (`top: 0; padding-top: var(--nav-top)`), der `top`-Übergang ist raus. Inhalt bleibt exakt an seiner Stelle, nur der night-Hintergrund reicht jetzt bis zur Viewport-Oberkante. Balkenhöhe ab 40 px 20 + 76 = 96 px = `--scroll-offset`, Anker unverändert. Unter 768 px `padding-top: 0` wie bisher (dort stand der Balken schon immer bei 0). Gemessen (1920, Lenis): Logo-Oberkante **20,2 px** bei Scroll 0, 100 und 600 (vorher 20,2 → 0,2); Balken 20–80 → 0–80; am Ende des Shrinks 0–96, Logo 41,2 hoch. Spec: Abschnitt 0 und neue Abweichung 12; Abweichung 11 ohne „Docking".

**Abnahme.** `npm run build` fehlerfrei, `motion-check.mjs` 1920: alle 69 Checks grün (Anker bei 96, `.is-scrolled` ab 40 px, Nav-Shrink am Pin-Ende unverändert).

**Nachtrag (gleicher Tag, Commit `f84f92c`): Zeitpunkt des Hintergrunds.** Ab 40 px lag das night-Band noch über dem hellen Hero-Foto und blendete sichtbar ein; Wunsch: erst, wenn die Textzeilen kommen. Der Trigger in `initNav` rechnet seinen Start jetzt als Funktion: mit Pin `hero-push.start + (end − start) × PUSH_THROUGH` (0,72 → bei 1080 hoch 1166 px, dort steht das Overlay auf 1, die Einblendung geht unter), ohne Pin und bei reduced motion Hero-Unterkante − Balkenhöhe (der Push-Trigger existiert dort nicht bzw. ist ungepinnt), mindestens 40. Wird bei jedem Refresh neu ausgewertet; `hero-push` misst mit `refreshPriority: 1` vorher. Gemessen 1920: Klasse aus bei 0/100/600/1000/1100 (Overlay 0,45 → 1), an ab 1170, Logo-Oberkante durchgehend 20,2. `motion-check.mjs` angepasst (Wheel-Probe erwartet nun *keinen* Hintergrund bei 300–480 px; neu: aus bei 0,72 − 60 px, an bei 0,72 + 20 px, an am Pin-Ende; reduced: aus bei 300, an bei innerHeight; < 768: an, sobald der Hero raus ist): 1920 alle Checks grün, 390 genau die sieben vorbestehenden Fehler, Nav-Checks grün. Spec Abschnitt 0 und Abweichung 12 ergänzt.


### Phase 7 – Animationen, Teil B/7 (Hero-Reveal zeitbasiert) · 2026-09-14 · Commit `880912b`

**Auftrag:** Eyebrow, Copy-Zeilen und Buttons erschienen beim Runter- und Hochscrollen nicht flüssig nacheinander.

**Ursache.** Der Reveal war Teil der gescrubbten Push-through-Timeline: pro Block nur 0.112 der Timeline = 181 px Scrollweg (Zeilen 127 px), Blöcke 136 px versetzt. Ein Mausrad-Schritt von 120 px deckte damit fast das ganze Fenster eines Blocks ab – die Zeilen sprangen statt zu gleiten, und rückwärts genauso.

**Behoben** in `animations.ts`: der Reveal ist eine eigene, zeitbasierte Timeline (`hero-reveal`, `toggleActions: 'play none none reverse'`, `invalidateOnRefresh`), die startet, sobald der Scroll die Position 0.72 des Pins passiert, und beim Zurückscrollen von der aktuellen Stelle rückwärts läuft. Blöcke 0.3 s versetzt (`REVEAL_GAP`), je 0.8 s `power4.out` (`REVEAL_DURATION`), Zeilen 0.15 s gestaffelt (`REVEAL_STAGGER`). Die Push-Timeline hält ihre Länge 1 über einen leeren Tween (`to({}, { duration: 1 − through })`), damit der Durchflug weiter 72 % des Pins belegt und der Rest Verweilstrecke ist. Neue Hilfsfunktion `heroRevealScroll()` liefert die Scrollposition des Reveals – der Nav-Trigger aus B/6 nutzt sie ebenfalls. Gefunden dabei: eine gestaffelte `fromTo` rendert nur den Startzustand des ersten Ziels vorab (zweite Zeile stand auf Opazität 1, unsichtbar hinter dem versteckten Container) – die Zeilen werden jetzt vorab per `gsap.set` auf y 135 / 0 gesetzt.

**Gemessen** (1920, Sonde alle 150 ms): vorwärts ab Scroll ≈ 1166 Eyebrow 0 → 0,46 → 0,83 → 0,97 → 1, Zeile 1 ab +0,3 s (0,51 → 0,85 → 0,97 → 1), Zeile 2 0,15 s dahinter, Buttons ab +0,6 s (0,51 → 0,86 → 0,97 → 1), fertig nach ≈ 1,5 s. Rückwärts (Scroll auf 900): Buttons 0,98 → 0,89 → 0,6 → 0, dann Zeile 2, Zeile 1, Eyebrow, fertig nach ≈ 1,4 s – gleiche Reihenfolge umgekehrt, keine Sprünge.

**Abnahme.** `astro check` ohne Fehler, `npm run build` fehlerfrei, `motion-check.mjs` 1920: alle Checks grün (Wartezeit der „landed"-Probe 1,8 → 3 s wegen Lenis-Lerp + 1,55 s Sequenz). Spec Abschnitt 1 (Timeline) angepasst.

### Phase 7 – Nachtrag: Founder-Bild verpixelt · 2026-09-14 · Commit `dfdbe4b`

**Befund.** `<Picture>` in `Founder.astro` setzte keine `quality`; Astro reicht dann nichts an sharp durch und AVIF fällt auf den sharp-Standard **50**. Das 713-px-Porträt lag bei 9,7 kB, die 1426-px-Variante (DPR > 1) bei 25,6 kB – Haut und Stoff glattgebügelt, Kanten blockig. **Behoben:** `quality={82}` (wie die JPG-Vorstufe aus `prepare-images.mjs`). Größen danach: 342 w 9,0 kB · 684 w 24,1 · 713 w 25,7 · 1426 w 74,3 kB AVIF (WebP 15/47 kB). Vorher/nachher-Zoom auf Uhr und Ärmel: Textur erhalten statt verschmiert. Die übrigen Fotos (Hero, Vision, Schwerpunkte, Spotlights) laufen noch mit dem Standard – falls dort dasselbe auffällt, gleiche Maßnahme.

### Phase 7 – Animationen, Teil B/8 (Hex-Cursor: Spotlight + Zellen-Trail) · 2026-09-14 · Branch `feat/hex-cursor-trail`, Commit `21ae313` – gemerged nach `main` 2026-09-14

**Auftrag:** Der Hex-Cursor wirkte wie ein statisches Raster, das per Maske freigelegt wird – zu großflächig (Radius 220), kein Nachziehen, kein Abklingen. Ersetzen durch Spotlight + Zellen-Trail.

**Befund vorab.** Die alte Umsetzung war bereits Canvas (`hex-cursor.ts` zeichnete das Raster im Umkreis und beschnitt es per `destination-in`-Radialverlauf), eine CSS-Masken-Lösung gab es nicht – es gab also keine toten Styles zu entfernen; `HexCursor.astro` (fixes Canvas, `z-index` 50, `pointer-events: none`, `mix-blend-mode: screen`) bleibt unverändert bis auf den Kommentar.

**Variante.** Ein fixes Viewport-Canvas für die ganze Seite statt eines Canvas je dunkler Fläche: die Sektionen sind fast überall night, das Hero-Foto/-Video und die Schwerpunkt-Kacheln dämpft der Screen-Blend; ein Canvas pro Sektion bräuchte Scroll-Synchronisation und Resize je Fläche und würde den Trail an Sektionsgrenzen abschneiden. Folge: das Raster liegt im Viewport-Raum (wie bisher) – beim Scrollen wandert der Schweif nicht mit dem Inhalt.

**Umsetzung** `src/scripts/hex-cursor.ts` (neu geschrieben, `animations.ts` lädt es weiter lazy nach `load`, jetzt nur bei `(pointer: fine) and (hover: hover)` ohne reduced motion):
- `TUNING` (ein Objekt oben in der Datei, im Dev-Server als `window.hexCursor` live änderbar): `SPOTLIGHT_RADIUS` 150, `FOLLOW_EASE` 0.12 (Lerp pro Frame, framerate-normiert), `IDLE_FADE_MS` 400, `RISE_MS` 120, `CELL_DECAY` 750 (Zeit bis 1 %, exponentiell), `CELL_FILL_ALPHA` 0.12, `CELL_STROKE_ALPHA` 0.9, `SPOTLIGHT_LINE_ALPHA` 0.35, `SPOTLIGHT_GLOW_ALPHA` 0.07, `ACTIVATION_POWER` 2. Geometrie und Farbe wie bisher (Kante 28, 1 px, `--c-magenta`).
- Pro Frame: Intensität hoch/runter, Lerp der Lichtposition, Zellen im Spotlight-Radius aktivieren (`intensity × (1 − d/R)²`, Maximum behalten), alle leuchtenden Zellen abklingen (`Map` nach Rasterindex, < 1 % gelöscht), zeichnen: Spotlight-Raster in einem Pfad → Radialmaske → Halo → Trail-Zellen je mit eigener Energie (Füllung + Kontur über `globalAlpha`). Nur Zellen im Umkreis werden durchlaufen.
- Schleife läuft, solange die Maus sich zuletzt bewegt hat (80 ms), Licht sichtbar ist oder eine Zelle leuchtet; `mouseleave` am `<html>` startet das Ausfaden, Tab-Wechsel räumt sofort. DPR ≤ 2, `resize` rechnet das Canvas neu.
- **Zwei Fallen dabei:** (1) der `requestAnimationFrame`-Zeitstempel des ersten Frames liegt *vor* dem `performance.now()` des Starts → `dt` negativ, Intensität unter 0, nichts gezeichnet; jetzt eine Uhr (`performance.now()` im Tick) und `dt` auf 0–64 ms geklemmt. (2) Beendete sich die Schleife nach dem ersten Frame (Intensität noch 0, keine Zelle), kam bei echten (CDP-)Mausereignissen nie ein zweiter Frame – synthetische `PointerEvent`s im selben Task verdeckten das; jetzt läuft die Schleife auch, solange `moving` gilt.

**Gemessen** (1920, headless): nach einem Schwenk 240 px Spotlight bei der Maus (60×60-Fenster voll belegt, max. Alpha 163), Schweif am Startpunkt (Alpha 121 → 35 nach 200 ms → 2 nach 500 ms), nach ≈ 1,2 s alles 0, 400 px entfernt nie etwas. Schnelle Lissajous-Bewegung 2,5 s (158 Moves, ≈ 160.000 leuchtende Pixel am Ende): Frame-Abstand Ø 16,6 ms, p95 18,1, max 18,3, **0 Long Tasks**. Sicht: `docs/screens/phase-7b-hex-trail-1920.png` (Vision-Sektion, Schweif über Foto und Night). Hex-Chunk **1.6 kB gzip** (vorher 1,0).

**Abnahme.** `astro check` 0 Fehler, `npm run build` fehlerfrei; `motion-check.mjs` Hex-Probe umgebaut (Schwenk in 8 Schritten per CDP; neu: Spotlight bei der Maus, Trail am Startpunkt, nichts in 400 px, unter der Nav/click-through/screen, **alles weg nach 1,6 s Ruhe**), 1920 alle Checks grün; no-js und reduced: Canvas bleibt `hidden`.

### Phase 7 – Animationen, Teil B/9 (Vision/Mission-Reveal gestaffelt) · 2026-09-14 · Commit `21ae313` (Komponenten – von der parallelen Hex-Cursor-Session mit eingesammelt) + Doku `05feb06`

**Auftrag:** Die Sektion „Vision und Mission" soll beim Scrollen dieselbe Einblendung bekommen wie die Textblöcke davor – alle Textelemente und das Bild, gleiche Werte, einmalig, reduced motion respektiert, kein Layout-Shift.

**Befund.** Der Section-Reveal (Punkt 4, `[data-reveal]`) lag bereits auf dem Vision-Bild, dem Vision-Textblock, dem Mission-Textblock und (gestaffelt) den Prinzipien – die Textblöcke blendeten aber als Ganzes ein. Gemessen 1920: Bild ab Scroll 1900, Textblock ab 2020, Mission-Text ab 2620.

**Umgesetzt** ohne Änderung an `animations.ts`: `.vision__text` und `.mission__text` tragen statt `data-reveal` das vorhandene `data-reveal-stagger` – dieselbe Utility, dieselben Werte (y 24 / mobil 16, 0,8 s, `power4.out`, `start: 'top 85%'`, `once`), Kinder 0,12 s versetzt. Damit kommen Eyebrow → H2 → Absätze nacheinander; das Bild bleibt eigenes `[data-reveal]`-Ziel und läuft, weil sein Trigger höher liegt, auf dem Desktop ≈ 120 px Scrollweg vor dem Text (Text und Bild versetzt), mobil steht es ohnehin darüber. In `Vision.astro` sind die beiden Absätze dafür direkte Kinder des Textblocks (Wrapper `.vision__copy` raus, Abstände als Geschwister-Ränder `--gap-head` bzw. 16 px) – Positionen unverändert (1920: Eyebrow 2934, H2 2993, Absatz 1 3168, Absatz 2 3296; 390: 1156 / 1206 / 1385 / 1557).

**Gemessen** (Sonde alle 100 ms). 1920, Scroll auf 2046: Bild 0,54 → 0,79 → 0,91 → 1, Eyebrow ≈ 0,1 s dahinter (0,12 → 0,56 → 0,78 → 0,92), H2 +0,12 s, Absatz 1 +0,12 s, Absatz 2 +0,12 s, alles nach ≈ 1,4 s auf 1 und y 0; Mission: Eyebrow → H2 → Body im gleichen Takt, parallel dazu die Prinzipien. 390 (Scroll 469): dieselbe Reihenfolge, fertig nach ≈ 1,0 s. Einmalig: nach Zurück auf 0 und erneutem Anfahren bleibt alles auf 1. Bildbox 650 px hoch vor dem Laden (`complete: false`) und danach identisch, mobil 260 – kein Layout-Shift (`aspect-ratio` in `Vision.astro`, `width`/`height` vom `<Picture>`). Reduced motion und ohne JS: alle 11 Ziele sofort sichtbar, kein Transform.

**Abnahme.** `astro check` 0 Fehler, `npm run build` fehlerfrei, `motion-check.mjs` 1920 (`--mode=all`): alle Checks grün („below-fold reveals start hidden" jetzt 8/8 statt 10/10 – zwei Blöcke sind Stagger-Ziele). Phase-7-Doc Punkt 5 ergänzt. Branch `feat/hex-cursor-trail`, nicht gemergt.

### Phase 7 – Animationen, Teil B/10 (Schwerpunkte: Hintergrundbild aus dem Figma) · 2026-09-14 · Commits `3386540` + `22d7504`

**Auftrag:** Die Sektion „Wo wir investieren" zeigte noch das Kran-Foto als Platzhalter; gewünscht ist das Bild aus dem Figma-Design.

**Umgesetzt.** Figma-Node `139:381` („hero-architecture-Photoroom 1", die Wireframe-Skyline) per MCP `download_assets` bei Scale 2 exportiert (2746×1664 PNG, enthält Figmas Beschnitt, steht exakt auf night 42/50/51) und als `_material/figma-export/schwerpunkte-export2x-139-381.png` abgelegt; die Rohdatei im Figma (1600×1000 PNG mit Alpha) liegt zur Referenz daneben. `prepare-images.mjs` kann jetzt Quellen außerhalb von `_material/Bilder` (`dir`) und JPEG ohne Chroma-Subsampling (`chromaSubsampling`) – das hält die dünnen cyanfarbenen Linien; Ausgabe `schwerpunkte.jpg` 2746×1664, 403 kB, Qualität 85 (`flatten` auf night für Quellen mit Alpha). Alle anderen Bilder bleiben bytegleich. `Schwerpunkte.astro`: `widths` 1373/2000/2746 (768 raus), `sizes` unter 1024 px **1700px** statt 100vw – dort ist der Rahmen viel höher als breit (390×1020) und Cover rendert die Querformat-Skyline auf ≈ 1680 css px; mit 100vw nahm Chrome die 768w-Variante und streckte sie 2,2×. Alt-Text in `de.json` angepasst, ASSETS.md-Zeile aktualisiert. Verläufe, Overlay und Layout unverändert (Spec Abschnitt 7 gilt weiter). Mobile-Frame `242:203` nutzt dasselbe Bild zentriert mit Cover – entspricht der bestehenden Mobile-CSS.

**Geprüft.** 1920: Bild 1373×832 zentriert, AVIF 1373w; Screenshot deckt sich mit dem Figma-Frame (Turm mittig hinter dem Kopf, Skyline unten, seitlich in night auslaufend). 390: 2000w-Variante bei 1699 css px gerenderter Breite, Box 390×1020. `npm run build` fehlerfrei.

**Hinweis.** Die Skyline wirkt auf der Seite etwas dunkler als im Figma-Screenshot, weil das Spec-Overlay night 60 % über allem liegt (Lesbarkeit) – bewusst nicht angefasst. Parallel lief eine zweite Session auf dem Branch; ihr abgebrochener Rebase (`git rebase --abort` setzt den Arbeitsbaum hart zurück) hat die noch nicht committeten Änderungen dieses Schritts einmal verworfen, sie wurden per Patch-Script neu angelegt und sofort committet.

### Phase 7 – Animationen, Teil B/11 (Warum K+: Scroll-Pin des Sliders) · 2026-09-14 · Branch `feat/warum-scroll-pin`, Commit `e66d9e6` – gemerged nach `main` 2026-09-14

_(B/8 Hex-Cursor-Trail und B/9 Vision/Mission-Reveal liegen auf `feat/hex-cursor-trail`; dieser Branch setzt auf `df89777` = main vor dem Founder-Nachtrag auf.)_

**Auftrag:** Die Sektion „Echte Baukompetenz" soll beim Scrollen festgehalten werden, der weitere Scrollweg treibt den Slider horizontal durch alle sechs Karten, dann löst sich die Sektion. Scroll-gekoppelt (kein Wheel-Hijacking), rückwärts symmetrisch, Zähler und Linie am selben Fortschritt; unter 1024 px und bei reduced motion nativer Slider wie bisher.

**Umsetzung** ohne GSAP-Pin – `position: sticky` in der Sektion als Scrollweg, `translateX` aus der Scrollposition:
- `WarumKplus.astro`: die Sektion (`data-warum`, bleibt `<section id="warum-kplus">` mit der einen H2 und Ziel der Nav-Trigger) enthält jetzt einen Block `.warum__sticky` (Kopf, Slider, Linie) und um die `<ul>` einen Wrapper `.warum__slider`, der im Pin-Modus die Reihe an den Viewport-Rändern beschneidet (`overflow-x: clip` – die `<ul>` selbst kann nicht clippen, weil sie verschoben wird). Klasse `.warum--pinned` (nur per JS): Sektion `height: var(--warum-pin-height)` und `scroll-margin-top`, Block `sticky; top: var(--warum-pin-top)`, Reihe `overflow: visible`, kein Scroll-Snap, `padding-inline-end` = Startabstand (statt der Nachlauffläche des nativen Sliders), `will-change: transform`. Ohne die Klasse ist alles wie vorher. Das Inline-Script ist nach `src/scripts/warum-slider.ts` gewandert.
- `src/scripts/warum-slider.ts` (neu): Linie + Zähler wie bisher (`warum:progress` bleibt), dazu der Pin bei `(min-width: 1024px) and (prefers-reduced-motion: no-preference)` (live per `matchMedia`, Rückbau beim Wechsel). Fortschritt = `(pinTop − Sektions-Oberkante) / travel`, geklemmt 0–1, direkt im passiven `scroll`-Listener (ein Ereignis pro Frame; der Block steht während des Pins still, ein Frame Versatz zum Lenis-Scroll ist damit unsichtbar); `translate3d` auf ganze Pixel gerundet (Schrift bleibt scharf, wenn die Reihe mittendrin steht). Zähler im Pin-Modus linear `round(p × 5)` (nativ weiter „nächste Karte an der Startkante" – dort schnappt die letzte Karte an die Startkante, im Pin steht sie rechts bündig, die Nächste-Karte-Regel zeigte am Ende 05). Neu gemessen bei `resize` und per `ResizeObserver` auf Block und Slider (Font-Load, Bilder); `tabindex` der Reihe im Pin-Modus weg (nichts mehr zu scrollen). Horizontales Wheel bewirkt im Pin nichts, die Reihe ist kein Scroll-Container mehr – nichts läuft doppelt.
- `animations.ts`: `warum:pin` (bubbelnd, `{ height, travel }`, bei jeder Änderung der Sektionshöhe) → `ScrollTrigger.refresh()` mit 0,2 s Entprellung, damit die Trigger darunter gegen die längere Seite messen (Font-Load kommt nach dem Refresh von `fonts.ready`); Kommentar am Slider-Init angepasst. Reveal der Karten (`ScrollTrigger.batch`, vertikal) und `[data-reveal]` am Kopf laufen unverändert vor dem Pin-Start; Hex-Cursor (fixes Viewport-Canvas) und Karten-Glow (Pointer-Rects) sind vom Sticky-Kontext unabhängig.

**Rechnung (Wrapper-Höhe / Scrollweg)** in `measure()`:
- `overflow` = rechte Kante der letzten Karte − linke Kante der Reihe + Endabstand (`--track-start`) − Sliderbreite. 1920: 6 × 636 + 5 × 32 + 160 − 1760 = **2376 px**; 1280: 6 × 560 + 5 × 32 + 64 − 1216 = **2368 px**.
- `travel` = `overflow × PIN_TRAVEL` (Konstante oben in `warum-slider.ts`, **1** = 1:1). Wer pro Karte mehr Scrollweg will, setzt z. B. 1,5 – die Reihe läuft dann pro Scroll-Pixel `1/PIN_TRAVEL` Pixel.
- Sektionshöhe = Blockhöhe + `travel` (1920: 600 + 2376 = 2976); Sticky-Oberkante `pinTop` = Viewporthöhe − Blockhöhe (1920×1080: 480, 1280×720: 86; negativ, falls der Block höher als der Viewport ist – dann bleibt die Unterkante mit Karten und Linie im Bild, der Kopf schaut oben raus). Der Pin greift also, sobald die Unterkante des Blocks die Viewport-Unterkante erreicht – nicht „Viewporthöhe + Überlauf", weil der Block (600) kürzer als der Viewport ist und sonst Leerlauf im Scrollweg entstünde. Folge: über dem gepinnten Block steht während des Pins eine leere night-Fläche (`pinTop` hoch).
- `scroll-margin-top` = max(0, `pinTop` − `--scroll-offset`), damit der Anker-Sprung (Lenis liest `scroll-margin`) genau am Pin-Start landet (1920: 384 → Oberkante 480; 1280×720: 0 → 96, 10 px hinter dem Start, Fortschritt 0).

**Gemessen** (headless, Sonde im Scratchpad). 1920×1080: Pin-Start bei Scroll 3669 (Block 480–1080), Mitte x −1188 / Zähler 03/06 / Linie 0,583, Ende x −2376 / letzte Karte rechts **1760 = Container-Rand** / 06/06 / Linie 1, Portfolio-Oberkante dort 1200 (1080 + Section-Gap); 300 px weiter ist der Block bei 180 (gelöst); zurück auf Mitte/Start wieder −1188 / 0. Anker-Klick aus der Nav: Oberkante 480, x 0, 01/06. Drei Wheel-Schritte à 400 über der Reihe: Seite 3669 → 4869, x −1200, 04/06; horizontales Wheel danach: unverändert. `scrollWidth` = `innerWidth` (kein Seitenüberlauf). 1280×720: Start 86, Ende 1216 = Container-Rand, Anker 95,5. Reduced motion 1920 und 800 px: keine Klasse, `overflow-x: auto`, `tabindex` 0, `scrollLeft` 668 bzw. 512 nach nativem Scroll, 02/06. `scroll-perf.mjs` 1920: 0 Long Tasks beim Scrollen, CLS 0,0019. Sicht: `docs/screens/phase-7b-warum-pin-{mid,end}-1920.png`.

**Abnahme.** `astro check` 0 Fehler, `npm run build` fehlerfrei; `motion-check.mjs` 1920 umgebaut (Anker erwartet mit Pin die Pin-Start-Kante, Reihe: elf Pin-Checks – Start 0/01, vertikales Wheel schiebt per Transform ohne `scrollLeft`, Block steht, Zähler/Linie folgen, kein Seitenüberlauf, horizontales Wheel wirkungslos, Ende bündig/06/Linie voll, Freigabe nach dem Weg, rückwärts; ohne Pin die alten Wheel-Checks): `--mode=js` alle grün, `nojs` und `reduced` grün. Spec Abschnitt 5 und Phase-7-Doc Punkt 7 ergänzt. Nicht gemerged.

**Zwischenfall.** Während der Umsetzung hat eine parallele Sitzung im selben Arbeitsverzeichnis die noch ungesicherten Dateien mit in ihren Founder-Commit `a02c6d5` auf main genommen und main danach wieder zurückgesetzt; die Umsetzung wurde deshalb aus diesem Commit in einen eigenen Worktree (`kplus-worktree-warum-pin`) auf diesen Branch übernommen und dort erneut gebaut und geprüft.

**Merge 2026-09-14:** `feat/hex-cursor-trail` (Hex-Cursor B/8, Vision/Mission B/9, Skyline B/10) per Fast-Forward und `feat/warum-scroll-pin` (B/11) per Merge-Commit nach `main`. Einziger Konflikt: die angehängten Einträge in dieser Datei, beide behalten. `astro check` 0 Fehler, `npm run build` fehlerfrei, `motion-check.mjs` 1920 auf dem zusammengeführten Stand alle Checks grün (inkl. der neuen Pin-Prüfungen des Sliders). Der Stash `stash@{0}` „WIP warum slider" ist damit vollständig überholt und kann gelöscht werden.


### Phase 7 – Animationen, Teil B/12 (Portfolio: Klick scrollt zum Spotlight) · 2026-09-14 · Commit `8b709c0`

**Auftrag:** Klick auf eine Beteiligungs-Kachel soll weich zum Bild-Text-Block scrollen, damit der Kunde sich orientieren kann.

**Umsetzung.** `portfolio.ts`: nach `select()` ruft der Klick-Handler `reveal()` – steht der Block (`[data-portfolio-panels]`) nicht komplett zwischen `--scroll-offset` und Viewport-Unterkante, wird `portfolio:scroll` (bubbling, cancelable, `detail { target, top }`) ausgelöst; `top` ist die Layout-Position über die `offsetParent`-Kette minus 96, also **ohne** den Reveal-Transform, der beim ersten Klick oft noch läuft (mit `getBoundingClientRect` landete Lenis 10 px zu tief). Fängt niemand das Ereignis, scrollt `window.scrollTo({ top })` nativ (`scroll-behavior` aus dem CSS: smooth, bei reduced motion auto). `animations.ts` (`initLenis`): `preventDefault` und `lenis.scrollTo(top)`. Tastatur-Auswahl scrollt nicht.
- **Scroll-Verankerung:** unter 1024 px sind die Panels inhaltshoch; sobald das alte Panel nach dem Fade `hidden` wird, schrumpft der Block (641 → 592 bei 390) und Chrome verschob die Scrollposition um 32 px – der Block stand bei 128 statt 96. `overflow-anchor: none` auf `.spotlights` (Portfolio.astro) behebt es, gemessen 96 vor und nach dem Wechsel.

**Gemessen** (Kachel 3 geklickt, Oberkante des Blocks alle 150 ms): 1920 mit Lenis 755 → 360 → 194 → 135 → 112 → 102 → 98 → 97 → **96** (≈ 1,2 s); 1920 reduced motion sofort 96; 390 mit Lenis 643 → 295 → 176 → … → **96**; zweiter Klick auf Kachel 5 bleibt in allen drei Fällen bei 96.

**Abnahme.** `astro check` 0 Fehler, Build fehlerfrei. `motion-check.mjs` +3 Checks: js „tile click scrolls the spotlight block under the nav" (96), js „no scroll when the spotlight block is fully visible", reduced „tile click jumps the spotlight block under the nav" (96). Spec Abschnitt 6 ergänzt. Die Klick-Prüfung scrollt den Block vorher unter die Falz (bei 1920 mit „+200" stand er schon ganz im Viewport und der Klick scrollte – korrekt – nicht) und wartet danach 1,5 s: ein natives `scrollTo` 400 ms nach einem Lenis-Scroll wird von Lenis nicht übernommen, das nächste `lenis.scrollTo` lief dann ans Seitenende. Nebenbei: die Hex-Sonde schwenkte bei 390 von x −45 los und maß den Trail außerhalb des Canvas – Start jetzt bei max(40, Mitte − 240). 1920 alle Checks grün, 390 genau die sieben vorbestehenden Fehler.

### Phase 7 – Nachtrag: CTA-Glow läuft in die Founder-Sektion · 2026-09-14 · Commit `7ceee22`

**Auftrag:** Der lime Glow hinter „Let's build beyond." endete an der Oberkante der CTA-Sektion in einer harten Kante über dem Eyebrow; er soll in die Founder-Sektion überlaufen.

**Ursache.** `.cta` hatte `overflow: hidden` und `isolation: isolate`: der 775-px-Kreis (top −130) wurde oben und unten an der Sektion abgeschnitten, und der eigene Stapelkontext hätte ihn beim Überlaufen *über* den Founder-Text gemalt.

**Behoben** in `Cta.astro`: `overflow-x: clip` statt `overflow: hidden` (nur die x-Achse, damit der Kreis bei 390 nicht die Seite verbreitert; `clip` erzeugt keinen Scroll-Container, die y-Achse bleibt `visible`), `isolation` entfernt. Der Glow mit `z-index: −1` liegt jetzt im Wurzel-Stapelkontext: über dem Night-Hintergrund von `html`, hinter dem Inhalt aller Sektionen (Founder hat keinen eigenen Hintergrund, der Footer auch nicht). Gemessen 1920: Glow-Box 459–1265 bei CTA-Oberkante 622, also 163 px in die Founder-Sektion hinein, Founder-Text bleibt unverändert lesbar darüber; 390: `scrollWidth` 390 = `innerWidth`, kein Überlauf. Spec Abschnitt 8 und Abweichung 13.

### Phase 7 – Animationen, Teil B/13 (Hex-Cursor nur auf der Night-Fläche) · 2026-09-14 · Branch `feat/hex-cursor-blocks` (Worktree `kplus-worktree-hex-blocks`), Commits `d7eae2c` + `b6b2380` (Hero-Nachtrag) – gemergt nach `main` 2026-09-14

**Auftrag:** Das Hex-Raster der Maus-Animation soll nur auf der dunklen Hintergrundfläche erscheinen, nicht über Bildern oder Blöcken.

**Befund vorab.** Ein reiner Stapel-Trick (Canvas mit `z-index: -1` hinter den Inhalt, Sektionen sind transparent über `html`-Night) reicht nicht: Karten und Tiles haben nur 4–5 % Weiß als Hintergrund (`--c-card-bg`, `--c-tile-bg`), das Raster würde durchscheinen. Also Maskierung im Canvas selbst.

**Umsetzung.**
- Markierung `data-hex-block` an: Hero-Sektion (Foto/Video), Vision-Foto (`<picture>`), alle sechs Warum-Karten inkl. Statement-Karte, sechs Portfolio-Tiles, sechs Spotlight-Panels (verborgene liefern ein leeres Rect und werden übersprungen), Schwerpunkte-Sektion (Skyline-Hintergrund), Founder-Foto – **22 Elemente**. Text, Buttons, Marquee, CTA und Footer bleiben frei.
- `hex-cursor.ts`: nach Spotlight und Trail radiert `eraseBlocks()` die Client-Rects aller markierten Elemente per `destination-out` aus dem Canvas (das Canvas ist fix und viewportgroß, Client-Koordinaten = Canvas-Koordinaten). Rects werden pro Frame gemessen (Blöcke scrollen, die gepinnte Kartenreihe verschiebt sich per Transform); Rects außerhalb des Viewports oder mit Größe 0 werden übersprungen. Eckenradius (`border-top-left-radius`, bei `resize` neu gelesen) per `roundRect`, Fallback `rect`. Läuft nur, wenn in dem Frame überhaupt etwas gezeichnet wurde.
- `HexCursor.astro`: nur Kommentare; `mix-blend-mode: screen` bleibt (über Night ergibt Screen weiter volles Magenta).
- Doku: Phase-7-Punkt 15 ergänzt.

**Gemessen** (1920×1080, headless, Sonde im Scratchpad). Schwenk von der Night-Fläche 40 px ins Vision-Foto: 40×40-Fenster 60 px links der Fotokante **1600** leuchtende Pixel, 4 px hinter der Kante **0**, 120 px tief 0. Portfolio-Tile (Radius 8): innen 0, im 2×2-Eckpixel außerhalb der Rundung 4 (die Rundung wird also nachgefahren), 40 px oberhalb 900. Schnelle Lissajous-Bewegung 2,5 s über Logo-Wall und Spotlight (8 Blöcke im Bild, 87 CDP-Moves, ≈ 38 500 leuchtende Pixel am Ende): 146 Frames, Abstand Ø 16,7 ms, p95 16,7, max 16,8, **0 Long Tasks** – die 22 `getBoundingClientRect`-Aufrufe pro Frame kosten nichts Messbares. Sicht: `docs/screens/phase-7b-hex-blocks-vision-1920.png` (Raster endet an der Fotokante), `phase-7b-hex-blocks-portfolio-1920.png` (Raster nur in den Fugen zwischen den Tiles, Spotlight frei), `phase-7b-hex-blocks-sweep-1920.png`.

**Abnahme.** `astro check` 0 Fehler, `npm run build` fehlerfrei; `motion-check.mjs` um zwei Checks erweitert („hex blocks marked" ≥ 20, „hex lattice lit beside the vision photo, none inside it") – JS-Modus 1920 gegen die Worktree-Vorschau (Port 4323, 4322 war belegt) **alle Checks grün**. Parallel lief Session `k-onepager-main-5f` im Hauptcheckout (ungesicherte `animations.ts`/`portfolio.ts`), deshalb eigener Worktree; beim Merge einziger Konflikt die angehängten Einträge in dieser Datei (B/12 war inzwischen vom Portfolio-Nachtrag belegt).

**Nachtrag (Hero-Copy).** Im Hero soll das Raster erscheinen, sobald die Copy zu sehen ist. Die Reveal-Timeline (`hero-reveal`, startet bei `PUSH_THROUGH` = 72 % des Pins, das Overlay ist dort schon seit 65 % voll Night) bekommt an Position 0 ein `set(hero, { attr: { 'data-hex-block': 'off' } })` – GSAP stellt beim Rücklauf (`toggleActions` … `reverse`) den Ausgangswert `"on"` wieder her, `gsap.matchMedia` beim Kontextwechsel ebenso (der Startwert muss nicht-leer sein: einen leeren String rendert GSAPs String-Tween beim Rücklauf nicht zurück, es bliebe `off` stehen). `eraseBlocks()` überspringt Blöcke mit `dataset.hexBlock === 'off'` (ein Attribut-Lesen pro Block und Frame). Unter 768 px (kein Pin, Copy von Anfang an über dem Foto) bleibt der Hero gesperrt. `motion-check.mjs`: zwei weitere Checks (Attribut `"on"` mitten im Durchflug, `off` am Pin-Ende). Sicht: `docs/screens/phase-7b-hex-blocks-hero-1920.png` (Pin-Ende, Raster über der Copy).

### Phase 7 – Nachtrag: Founder-Bild mobil, Kopf abgeschnitten · 2026-09-14 · Commit `c38b05b`

**Befund.** Unter 1024 px wird das Hochformat-Foto (1600 × 2399) im Querformat-Rahmen 342 × 247 gezeigt; mit `object-position: 50% 20%` lag der Ausschnitt 53 px zu tief (20 % von 266 px Überhang) und der Kopf war oben abgeschnitten. **Behoben:** `object-position: 50% 0` im 1023er-Block von `Founder.astro`. Gegengeprüft per Style-Injektion vor dem Build: 0 % zeigt den Kopf bei 390 und 800 komplett mit wenig Luft, 6 % schneidet schon den Scheitel an. Desktop (713 × 515, 15 %) unverändert.
